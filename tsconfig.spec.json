{
  "compilerOptions": {
    "types": ["vitest/globals", "node"]
  },
  "extends": "./tsconfig.json",
  // The zone project compiles against `tsconfig.zone.json`, so its files are not in here.
  "include": ["src/**/*.spec.ts", "src/test-setup.ts", "src/**/*.d.ts"]
}
