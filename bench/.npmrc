# The competitors declare `vitest` as a peer. Auto-installing it here would put a SECOND copy of
# @vitest/spy next to the root one — two mock registries, so pruneMockRegistry cleans only one and
# the bench OOMs. Peers must resolve upward to the root install.
legacy-peer-deps=true
