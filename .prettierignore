dist
coverage
node_modules
.cache
package-lock.json
CHANGELOG.md


# Scratch, caches and agent worktrees: not shipped, not reviewed, and full of captured
# third-party text that reformatting would silently rewrite.
.claude
tasks

# Formatted by `npm run agents:sync`: prettier first, then the table padding stripped, which prettier
# would put back. `agents:sync:check` is their format check.
AGENTS.md
agent-docs
