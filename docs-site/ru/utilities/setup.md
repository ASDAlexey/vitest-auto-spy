---
title: Гигиена тестового прогона
description: setupAutoSpy() — восстановление свойств, сброс реестра моков, обнаружение дублирующихся копий, забытые таймеры, реджекты и вывод в консоль, охрана глобальных патчей и строгий пресет — одним вызовом.
---

# Гигиена тестового прогона

```ts
// vitest.setup.ts
import { setupAutoSpy } from 'vitest-auto-spy/setup';

setupAutoSpy();
```

Один вызов вместо тех кусочков гигиены, которые иначе каждый проект собирает руками, — каждый из них
дёшево поставить и дорого диагностировать, когда его нет. Первые три включены по умолчанию,
остальные — переключатели, потому что они меняют то, что видит проверяемый код.

## 1. Восстановление пропатченных свойств {#_1-restoring-patched-properties}

`vi.restoreAllMocks()` знает про спаи, а не про свойства, которые переопределили
[`mockReadonlyProp` / `mockValueProp`](../adapters/angular#signal-readonly-property-mocking). При
`isolate: false` невосстановленный патч на глобале, прототипе или синглтоне утекает прямиком в
следующий файл. `setupAutoSpy()` регистрирует `restoreMockedProps()` в глобальном `afterEach`.

**И подстраховку в `onTestFinished` за ним**, потому что выполнение `afterEach` не гарантировано.
Vitest вызывает хуки `afterEach` в **обратном** порядке регистрации, так что зарегистрированный
setup-файлом оказывается _последним_, а любой хук, зарегистрированный самой спекой, — то есть
выполняющийся первым — утаскивает всю цепочку за собой, когда бросает:

```ts
// в файле спеки, а значит, до хука библиотеки
afterEach(() => vi.restoreAllMocks()); // ← бросает, и уборка под ним не происходит никогда
```

Это не гипотеза. Одна спека годами держала ровно эту строку; переезд на
`provideAutoSpy(LayoutStateService, { gettersToSpyOn: [...] })` сделал так, что восстановленный
геттер начал возвращать `undefined`, `ngOnDestroy` вызвал его как сигнал, и полученный `TypeError`
оборвал хук. Патч уехал дальше, а падение всплыло в **другом `describe`** как ошибка шаблона про
null-профиль. С самодельным `vi.fn()`, который он заменил, восстановленный геттер оставался
вызываемым, так что мина всё это время лежала невидимой.

`onTestFinished` выполняется после цепочки `afterEach` и делает то же, что делала она, так что
подстраховка возвращает свойства на место и предупреждает — называя тест, сколько патчей она
вернула и какой хук бросил, в том тесте, где это случилось, а не двумя тестами позже. На обычном пути это стоит одного булева
значения: она ничего не делает, если хук не пропустили.

`countMockedProps()` экспортируется для сюит, которые предпочтут это проверять:

```ts
afterEach(() => expect(countMockedProps()).toBe(0));
```

## 2. Одна копия библиотеки в процессе {#_2-one-copy-of-the-library-in-the-process}

Две копии держат два набора консольных спаев и два реестра, так что ассерт проверяет спай, который
никогда не подменял консоль, вызванную проверяемым кодом, — а симптом читается как «тесты падают в
зависимости от порядка файлов». Проверка роняет прогон отчётом, называющим обе копии и что делать с
каждой из причин: вторая установка либо одна установка, загруженная и в ESM-, и в CommonJS-форме.

```ts
import { describeDuplicateCopies, getPackageCopies } from 'vitest-auto-spy/setup';

getPackageCopies(); // зарегистрированные копии, для собственной отчётности
describeDuplicateCopies(); // человекочитаемый отчёт или undefined, когда копия одна
```

Обе функции экспортируются и из корневой точки входа.

## 3. Очистка реестра восстановления у раннера {#_3-draining-the-runner-s-restore-registry}

Каждый `vi.spyOn` добавляет запись, которую убирает только `vi.restoreAllMocks()`; при общей среде
этот список растёт на весь прогон. `restoreMocks: true` вычищает его после каждого теста.

### Clear, reset и restore применительно к авто-спаю {#clear-reset-and-restore-applied-to-an-auto-spy}

С 4.1 спай метода — не `vi.fn()`, а собственный мок библиотеки, поэтому резонно спросить, доходят ли
до него три конфиг-флага раннера. Доходят — ровно настолько же, насколько до `vi.fn()`. Vitest
применяет флаги через `vi.clearAllMocks()`, `vi.resetAllMocks()` и `vi.restoreAllMocks()` перед
каждым тестом, а библиотека регистрирует один собственный `vi.fn()`, чьи `mockClear` и `mockReset`
разом проходят по всем авто-спаям воркера. Под
[`setSpyEngine('runner')`](/ru/core/performance#the-spy-engine) спаи — это `vi.fn()`, и флаги
достают их напрямую. Таблица одинакова для обоих движков, замерено на Vitest 5:

| конфиг-флаг    | по умолчанию              | записанные вызовы | `mockReturnValue` / `mockImplementation`           | правила `calledWith(…)` | `vi.spyOn` на настоящем объекте     |
| -------------- | ------------------------- | ----------------- | -------------------------------------------------- | ----------------------- | ----------------------------------- |
| `clearMocks`   | `true` начиная с Vitest 5 | очищаются         | остаются                                           | остаются                | вызовы очищены, спай на месте       |
| `mockReset`    | `false`                   | очищаются         | сбрасываются — метод или геттер отвечает undefined | остаются                | снова отвечает настоящая реализация |
| `restoreMocks` | `false`                   | не трогаются      | не трогаются                                       | не трогаются            | снят, на месте настоящий член       |

Что это значит на практике:

- **`clearMocks` на Vitest 5 уже делает свою работу.** Там он включён по умолчанию, доходит до
  каждого авто-спая и сохраняет всё, чем их настроили.
- **`mockReset` стирает настройку, сделанную вне `beforeEach`.** Он срабатывает перед каждым тестом —
  после `beforeAll` и после тела `describe`, — так что настроенный там дубль в каждом тесте отвечает
  `undefined`. Это та же ловушка, что и с настроенным там `vi.fn()`, и причина собирать дубли в
  `beforeEach`. Правила `calledWith(…)` его переживают;
  [`resetAutoSpy(double)`](/ru/core/control-helpers#resetting-spies-—-clearautospy-resetautospy)
  сбрасывает и их.
- **`restoreMocks` авто-спай не трогает никогда.** Начиная с Vitest 4 он откатывает только
  `vi.spyOn`, а у дубля, собранного из класса, под спаем нет настоящего члена, который можно вернуть.
  Этот флаг не сбрасывает ваши дубли; он возвращает на место пропатченные настоящие объекты — то же
  самое после каждого теста делает `setupAutoSpy({ restoreMocks: true })`.

Авто-спаю, собранному в `beforeEach`, не нужен ни один из трёх: следующий тест соберёт свежий.
`setupAutoSpy()` ничего не добавляет поверх флагов: сам он дубли не очищает и не сбрасывает, потому
что флаги — это контракт раннера. Для дубля, который живёт дольше теста, есть
[`resetAutoSpy` и `clearAutoSpy`](/ru/core/control-helpers#resetting-spies-—-clearautospy-resetautospy).

## 4. Отмена таймеров, переживших свой файл {#_4-cancelling-timers-that-outlive-their-file}

По желанию, и имеет смысл только при `isolate: false` — там каждый файл спеки в воркере делит один
набор глобалов. `setTimeout`, который компонент завёл и не очистил, переживает создавший его файл:
колбэк срабатывает, пока **другой** файл в середине теста, против моков и DOM, которые больше не
совпадают, а раннер обвиняет тот файл, который случайно выполнялся в этот момент.

`requestAnimationFrame` — та половина, которую упускают. Планировщик zoneless change detection в
Angular гоняет таймаут наперегонки с кадровым колбэком, так что у компонента, уничтоженного в конце
одного файла, вполне может остаться поставленный кадр, — а всплывает потом внутренняя жалоба Angular
(планировщик выполняет watch'и во время планирования, чтение сигнала в фазе оповещения), приписанная
ни в чём не повинному коду.

```ts
setupAutoSpy({ strayTimers: true });
```

Это оборачивает четыре планировщика один раз на воркер и подметает всё оставшееся в `afterAll`.
Части экспортируются для сюиты, которая хочет подметать в другом месте — или хочет, чтобы утечка
**роняла** прогон, а не молча прибиралась:

```ts
import { cancelStrayTimers, countStrayTimers, trackStrayTimers } from 'vitest-auto-spy/setup';

const stop = trackStrayTimers(); // идемпотентно; возвращает отмену, которая заодно всё отменяет
afterEach(() => expect(countStrayTimers()).toBe(0));
afterAll(() => {
  const cancelled = cancelStrayTimers(); // сколько пришлось отменить

  if (cancelled > 0) {
    process.stdout.write(`${cancelled} timer(s) outlived this file\n`);
  }
});
```

Каждая принимает необязательный host, по умолчанию — настоящие глобалы, так что тест может
подсунуть свой объект-заменитель. При `isolate: true` это почти no-op — среда всё равно
выбрасывается после каждого файла. `trackStrayTimers()` идемпотентен и там, где host отказывает в
одном из пяти патчей: частичная установка откатывается целиком, а не остаётся стоять без отмены и
без записи в реестре.

::: warning Таймер, созданный под фейковыми таймерами, ему не виден
`vi.useFakeTimers()` кладёт поверх обёрток собственный `setTimeout`, так что всё, что раздают
фейковые часы, проходит мимо трекинга целиком. Поэтому `expect(countStrayTimers()).toBe(0)` в любом
файле на замороженных часах не проверяет ничего, включая
`setupAutoSpy({ strayTimers: true, globalFakeTimers: true })`, — а `vi.getTimerCount()` показывает
очередь самих фейковых часов, и вместе эти двое не складываются.
:::

Отменённым считается больше, чем тот handle, который записал трекер. Node раздаёт объект `Timeout`,
и библиотека, которая хранит id числом, отменяет через `clearTimeout(+handle)` либо зовёт
`handle.close()` у самого объекта; и то и другое читается как отмена, а не выдаётся за забытый
колбэк со стеком, указывающим на здоровый код. `promisify(setTimeout)` тоже продолжает работать —
обёртка переносит на себя ту особую реализацию `promisify`, которую Node вешает на настоящий
планировщик и на которую опирается `const sleep = promisify(setTimeout)`, вычисленный на импорте.

Не каждый таймер файла поставлен его кодом. jsdom отвечает на каждый `setItem`, `removeItem` и
`clear` у Web Storage настоящим `setTimeout(…, 0)`, который рассылает событие `storage`
(`living/webstorage/Storage-impl.js`), поэтому у файла, который только пишет в `localStorage`, всё
равно висят таймауты — а полностью синхронный файл может дойти до `afterAll`, так их и не дождавшись.
Собственная проба хранилища у библиотеки (раздел 14) идёт под `withoutStrayTimerTracking` и файлу
никогда не засчитывается; тот же хелпер выводит из счёта и собственную подготовку сюиты:

```ts
import { withoutStrayTimerTracking } from 'vitest-auto-spy/setup';

withoutStrayTimerTracking(() => seedStorage()); // то, что здесь запланировано, не считается и не отменяется
```

`onStrayTimers` — тот же счёт, но не выходя из `setupAutoSpy`, и вдобавок **откуда взялся каждый
забытый колбэк**: `timers` перечисляет их все — вид, задержку таймаута или интервала, spec-файл,
который выполнялся в момент планирования, тест, который тогда шёл (`test`, в виде `suite > test`), —
или `outsideTest: 'import' | 'hook'`, если теста не было, — и до пяти фреймов планирующего вызова.
Фреймы начинаются с вызывающего кода: сначала те, что вне `node_modules`, фреймы зависимостей — если
других нет, и никогда собственные фреймы библиотеки — ни из исходников, ни из `dist`.
`onStrayTimers: 'throw'` роняет файл отчётом, который называет файл, каждый забытый колбэк и одно
действие; обработчик, который печатает своё сообщение, должен печатать те же `kind`, `delay`, `test`
и `frames[0]`:

```text
[vitest-auto-spy] src/app/cart.component.spec.ts left 1 timer pending when it ended:
  - setTimeout 5000 ms, scheduled in "CartComponent > polls" at src/app/cart.component.spec.ts:14:5
It was cancelled so it cannot fire in the next file. Clear it in the test that scheduled it — clearTimeout, unsubscribe, fixture.destroy() — or run it out with fake timers before the test ends.
```

```ts
setupAutoSpy({ strayTimers: true, onStrayTimers: 'throw' }); // вид, задержка, файл и первый фрейм каждого

setupAutoSpy({ strayTimers: true, onStrayTimers: ({ cancelled }) => expect(cancelled).toBe(0) });

setupAutoSpy({
  strayTimers: true,
  onStrayTimers: ({ timers }) => expect(timers).toEqual([]), // дифф падения назовёт файл и фреймы
});
```

Файл — то, что позволяет найти колбэк, записанный не на тот файл: запланированный после подметания
предыдущего файла считается против следующего, а его `file` говорит, кто на самом деле его
запланировал. Стек снимается в момент планирования — не больше сорока фреймов ниже обёртки трекинга,
чтобы zone или планировщик rxjs посередине не съели их все, форматируются только те, что оказались
забытыми, — а `describeStrayTimers()` возвращает тот же список набору,
который подметает сам.

Этот снимок стека и есть цена `strayTimers`. Пара `setTimeout` + `clearTimeout` на настоящих таймерах
Node занимает 70 ns без трекинга, 116 ns с трекингом в 5.5.0 и около 1.75 µs с трекингом сейчас, а
висящий таймер держит примерно на 0.9 kB больше, пока не сработает или не будет убран (Node v24.19.0,
Apple M4 Max). На неглубоком стеке ограничение ничего не меняет: V8 платит около 0.9 µs за любой
стек вообще. За глубокой цепочкой фреймворка каждый снятый фрейм стоит около 60 ns, и таймер оттуда
обходится примерно в 3 µs там, где двенадцать фреймов стоили 1.4 µs. Файл, планирующий 10 000 таймеров, платит около 16 ms за то, чтобы
знать, откуда взялся каждый. Имя теста добавляет одно чтение свойства в момент планирования — само
имя собирается только для забытого колбэка — и на фоне снимка стека не видно: отслеживаемая пара
заняла 4.0 µs и до, и после, оба замера с coverage.

### Вместе с `--detect-async-leaks` из Vitest 4.1 {#with-vitest-4-1-s-detect-async-leaks}

::: warning Они гасят друг друга, и побеждает тихий
`detectAsyncLeaks` запоминает каждый асинхронный ресурс, созданный файлом, и, когда файл кончился,
спрашивает у каждого, держит ли его ещё кто-нибудь. Подметание идёт в `afterAll` — **до** этого
вопроса, — так что каждый отменённый им таймер отвечает «нет», и файл, который течёт таймерами,
считается не текущим вовсе.
:::

Отменять всё же правильнее по умолчанию: колбэк, срабатывающий во время более позднего файла, —
более дорогое падение, и именно ради него существует `strayTimers`. Поэтому, когда включены оба и
`onStrayTimers` не задан, подметание печатает в stderr тот же отчёт, которым роняет `'throw'`, —
файл, число и первые три таймера с тестом, который запланировал каждый, — и закрывает его одним
предложением: этих таймеров нет в отчёте Vitest «Async Leaks». `onStrayTimers` получает их все. Собственный отчёт Vitest при выключенном `strayTimers` показывает фреймом кода на
`setTimeout` в спеке: обёртки планировщиков в библиотеке идут через `vi.defineHelper`, так что
фреймы внутри `vitest-auto-spy` из стека выбрасываются, а не показываются вместо спековских.

```
⎯⎯⎯⎯⎯⎯⎯ Async Leaks 1 ⎯⎯⎯⎯⎯⎯⎯⎯

Timeout leaking in src/app/cart.component.spec.ts
  12|   it('polls', () => {
  13|     component.startPolling();
  14|     setTimeout(() => refresh(), 60_000);
     |     ^
```

Предупреждение уходит в stderr, а не в `console.warn`, намеренно: подметание идёт после последнего
теста файла, а Vitest приписывает перехваченный вывод консоли той задаче, которая его породила, —
задачи уже нет, и строка теряется.

## 5. Как удержать прогон подальше от сети {#_5-keeping-the-run-off-the-network}

По желанию, и существует оно ради прогона, который зелёный и всё равно падает.

В jsdom нет `fetch`, так что под ней компонент, тянущийся за удалённым ассетом, безвреден и сюита
ничего не замечает. happy-dom его реализует, и тот же компонент начинает слать настоящие запросы —
загрузчик иконок тянет все SVG с CDN, конфиг-сервис опрашивает эндпоинт. Тесты по-прежнему проходят,
потому что ничего из того, что они проверяют, от ответа не зависит. А прогон — нет:

```text
 Test Files  260 passed (260)
      Tests  2257 passed (2257)

Vitest caught 8 unhandled errors during the test run.
DOMException [AbortError]: The operation was aborted.
```

Раннер обрывает всё, что в полёте, когда сносит среду, и эти обрывы прилетают необработанными
реджектами уже после итога. Код возврата 1, и ни одного названного теста — потому что ни один тест не
упал.

```ts
setupAutoSpy({ blockNetwork: true });
```

`fetch` тогда сразу реджектится, называя, что именно запрашивали и какой тест, — а это как раз то,
чего стектрейс вам не скажет, — и строку, которая на запрос ответит:

```text
[vitest-auto-spy] fetch is stubbed in unit tests — GET https://cdn.example.test/sprite.svg. The test "icons > loads the sprite" requested it, and blockNetwork() refused it: unit tests stay off the network. Answer it in this test: vi.spyOn(globalThis, 'fetch').mockResolvedValue(stubResponse({ body: … })).
Docs: https://asdalexey.github.io/vitest-auto-spy/utilities/setup#_5-keeping-the-run-off-the-network
```

Заблокированный `XMLHttpRequest` несёт в `statusText` свою строку:
`[vitest-auto-spy] XMLHttpRequest is stubbed in unit tests — GET <url> blocked. Stub it in this test, or blockNetwork({ xhr: 'empty' }) if nothing reads the reply.`

Ничего не покидает машину, прогон перестаёт зависеть от доступности хоста, а проверяемый код идёт
ровно по той ветке, по которой пошёл бы при неудачном запросе. Спека, которой `fetch` действительно
нужен, подменяет его как и раньше — это пол, а не потолок. Заглушка ставится на каждый тест
(`restoreProps` её снимает), а `blockNetwork()` экспортируется для сюит, которым нужен более узкий
охват.

`fetch` — это только половина сети, а вторую половину jsdom реализует полностью. Множество библиотек
так и не ушли от `XMLHttpRequest` — `rmp-vast` пингует каждый VAST-трекер через самодельный, — так
что сюита с уже включённым `blockNetwork: true` всё равно ходила в интернет, по пингу на квартиль на
рекламу на тест, и печатала jsdom'овский `AggregateError at Object.dispatchError` на каждое
неудавшееся соединение. Печатает ли это зелёный прогон, зависит от того, есть ли у машины маршрут
наружу, — а это не то свойство, которое должно быть у тестовой сюиты.

Каждый канал, который реализует среда, закрыт по умолчанию. Объект сужает это:

| опция    | по умолчанию | что делает                                                                     |
| -------- | ------------ | ------------------------------------------------------------------------------ |
| `fetch`  | `true`       | `fetch` реджектится, называя, что запрашивали                                  |
| `xhr`    | `'reject'`   | как отвечать на перехваченный `XMLHttpRequest` — или `false`, чтобы не трогать |
| `beacon` | `true`       | `navigator.sendBeacon` отвечает `false`, там где он в среде есть               |

`'reject'` роняет запрос так же, как это делает недоступный хост: `readyState` 4, `status` 0,
событие `error` и маркер в `statusText` — единственном строковом канале, который есть у неудавшегося
запроса. `'empty'` вместо этого отвечает статусом 200 и пустым телом — именно этого хочет запрос,
ответ на который никто не читает:

```ts
setupAutoSpy({ blockNetwork: { xhr: 'empty' } }); // пинги трекера, отвеченные и беззвучные
```

`data:`-URL пропускается, и он единственный: это схема, из которой спека раздаёт собственные
фикстуры, и единственная, на которую DOM отвечает без сокета. **Относительный** URL исключением не
является — DOM разрешает его относительно origin документа, так что спека, которая ходит в `/config`
и проходит, держится на том, что на этом порту никто не слушает.

`WebSocket` и `EventSource` сознательно не трогаются. Их отказ — это событие на объекте, который код
хранит и переподключает, так что нет такого ответа сплошной заглушки, который сам не был бы
изменением поведения; для спеки, у которой такой объект есть, инструмент —
[`stubConstructor`](/ru/utilities/constructor-doubles).

### Ответ заглушенному `fetch` — `stubResponse` {#answering-a-stubbed-fetch-—-stubresponse}

Спека, которой нужна ветка успеха, подменяет `fetch`, и ей нужно вернуть `Response`. Обычно его
собирают руками — `{ ok: true, json: async () => data } as Response`: каст поверх объекта, в котором
есть два члена из тех, что может прочитать проверяемый код. `stubResponse` собирает настоящий, из
собственного конструктора среды, без каста:

```ts
import { stubResponse } from 'vitest-auto-spy/setup';

vi.spyOn(globalThis, 'fetch').mockResolvedValue(stubResponse({ body: { id: 1, name: 'Ada' } }));
vi.spyOn(globalThis, 'fetch').mockResolvedValue(stubResponse({ ok: false, status: 404 }));
```

| поле         | по умолчанию                     | что делает                                                                                     |
| ------------ | -------------------------------- | ---------------------------------------------------------------------------------------------- |
| `body`       | без тела                         | обычный объект, массив, число, boolean, `null` → JSON с `application/json`; остальное как есть |
| `status`     | `200`, или `500` при `ok: false` | статус; статус, который платформа не принимает (`0`, `600`), бросает её собственную ошибку     |
| `ok`         | выводится из `status`            | сокращение для класса статуса; `ok`, который расходится со `status`, бросает                   |
| `statusText` | `''`                             | как передан                                                                                    |
| `headers`    | —                                | любой `HeadersInit`; заданный здесь `content-type` побеждает JSON-овый                         |
| `url`        | `''`                             | то, что читает `response.url`, — fetch-бэкенд Angular отдаёт его как URL запроса               |

Строка отправляется как есть, а не как JSON-строка. Тела `Blob`, `ArrayBuffer`, типизированный
массив, `FormData`, `URLSearchParams` и `ReadableStream` уходят в конструктор нетронутыми — с одной
оговоркой под jsdom: его `Blob` и `FormData` — собственные jsdom'овские, и `Response` из Node их не
читает. Там передавайте строку или байты.

**`null` — это JSON-литерал, а «без тела» — это `undefined`.** Бэкенд, который отвечает «здесь
ничего нет» JSON-овым `null` — пустой логин, отсутствующий профиль, — это реальная форма, и
проверяемый код разбирает её как любую другую:

```ts
await stubResponse({ body: null }).json(); // → null, content type application/json
await stubResponse({ body: undefined }).text(); // → '', без content type, .json() реджектится
await stubResponse({}).text(); // → то же самое: опущенное тело — это отсутствие тела
```

**Это разворот выпущенного контракта.** Раньше `null` означал «без тела» и был неотличим от
опущенного поля, так что `.json()` на нём бросал `Unexpected end of JSON input`, тогда как `0`,
`false`, `[]` и `{}` спокойно ходили туда и обратно, — и это была ловушка, а не предпочтение: ничто
не подсказывало читателю, что именно этот литерал ведёт себя иначе. Способ сказать «без тела» один,
и за ним и так тянется каждый вызывающий, — поэтому `null` свободен значить то, что он значит в
JSON. Спека, которая опиралась на старое прочтение, убирает поле или пишет `body: undefined`.
Единственное место, где об этом говорится вслух, — `{ body: null, status: 204 }`: 204, 205 и 304
тела не несут, так что теперь бросается ошибка по имени поля, а не конструкторное «Response with
null body status cannot have body» про поле, у которого старое значение было противоположным.

**Тело `Response` читается один раз.** `mockResolvedValue(stubResponse(…))` отдаёт один и тот же
объект на каждый вызов, так что второй `response.json()` реджектится с «Body is unusable». Заглушка,
которая отвечает больше чем на один запрос, собирает ответ на каждый вызов:

```ts
vi.spyOn(globalThis, 'fetch').mockImplementation(async () => stubResponse({ body: user }));
```

Работает везде, где у рантайма есть глобальный `Response`: Node, jsdom (своего у неё нет, так что в
области видимости — нодовский), happy-dom, Bun и `node:test`. В остальных местах бросает `TypeError`,
называющий недостающий конструктор. Каждая из трёх его ошибок заканчивается починкой для своего
случая — убрать `ok`, опустить `body`, запускать там, где `Response` есть, — и ссылкой на этот раздел.

### Рядом с MSW {#next-to-msw}

`blockNetwork` и `setupServer()` из [MSW](https://mswjs.io) патчат одни и те же глобалы, так что
раньше победителя определял порядок. `server.listen()` сидит в `beforeAll`, который выполняется до
потестового `beforeEach` из `setupAutoSpy()`, поэтому заглушка `fetch` ложилась **поверх**
перехватчика MSW, и все обработчики `fetch`-запросов выключались: запрос, который спека замокала,
реджектился с `fetch is stubbed in unit tests`. Это касается и `HttpClient` из Angular, у которого в
актуальных версиях бэкенд по умолчанию — `fetch`: `withFetch()` помечен устаревшим как лишний, а
`withXhr()` возвращает `XMLHttpRequest`.

Теперь `blockNetwork` не трогает `fetch`, пока его держит перехватчик из `@mswjs/interceptors` —
`setupServer()` из MSW, а также nock 14, построенный на том же пакете, — и снова блокирует его, как
только `server.close()` снимает перехватчик. С `XMLHttpRequest` такой осторожности не нужно: MSW
строит под собой настоящий класс, так что запрос, который он не обработал, всё равно доходит до
заблокированных `open` и `send` и падает с маркером в `statusText`. Замерено на MSW 2.15, под jsdom и
happy-dom, на `fetch`, `XMLHttpRequest` и `HttpClient` с `withFetch()` и `withXhr()`:

| запрос                               | обработан MSW | не обработан MSW                   |
| ------------------------------------ | ------------- | ---------------------------------- |
| `fetch`, `HttpClient` (fetch)        | ответ MSW     | решает `onUnhandledRequest` из MSW |
| `XMLHttpRequest`, `HttpClient` (XHR) | ответ MSW     | заблокирован, URL назван           |

Так что под MSW пол для `fetch` — собственный пол MSW, а `onUnhandledRequest: 'error'` — не совсем
пол. Он делает исключение для каждого URL, путь которого похож на статический ассет, — `.svg`,
`.png`, шрифты, `.css`, `.js`, а заодно и `.json` — и молча выпускает такой запрос в сеть: ровно тот
загрузчик иконок, с которого начался этот раздел. Жёстким пол делает обработчик-ловушка,
зарегистрированный последним:

```ts
import { HttpResponse, http } from 'msw';
import { setupServer } from 'msw/node';

export const server = setupServer(
  ...handlers,
  http.all('*', () => HttpResponse.error()), // всё, что пропустили обработчики выше, падает без сети
);

beforeAll(() => server.listen({ onUnhandledRequest: 'error' }));
afterEach(() => server.resetHandlers());
afterAll(() => server.close());
```

`server.use(…)` в спеке добавляет свои обработчики в начало, так что ловушка остаётся последней;
`resetHandlers()` её сохраняет, потому что она передана в `setupServer`. Держите рядом
`blockNetwork: true` для каналов, которые MSW не покрывает, — `sendBeacon` и любого файла, который
сервер вообще не запускает.

## 6. Как вернуть глобалы таймеров, которые фейки унесли с собой {#_6-putting-back-timer-globals-the-fakes-took-with-them}

Включено по умолчанию, потому что оно может только чинить.

`vi.useRealTimers()` читается как обратная операция к `vi.useFakeTimers()`, и в обычном
Node-реалме так и есть. Под DOM-средой — нет: `@sinonjs/fake-timers` восстанавливает глобал, присваивая
оригинал обратно, **когда тот был собственным свойством глобального объекта**, а иначе удаляет его. В
happy-dom `Date` наследуется из реалма среды, так что деинсталляция убирает его совсем.

При `isolate: true` этого никто не заметит. При `isolate: false` следующий файл того же воркера
встречает реалм без `Date` и умирает внутри собственного `useFakeTimers` Vitest, за несколько файлов
от того, кто фейки поставил:

```text
TypeError: Cannot read properties of undefined (reading 'now')
 ❯ hijackMethod node_modules/@sinonjs/fake-timers/src/fake-timers-src.js
 ❯ Object.useFakeTimers node_modules/vitest/dist/chunks/vi.js
 ❯ src/app/billing/invoice.component.spec.ts:24:6
```

Файл в этом стеке — просто тот, который выполнялся следующим.

Настоящие глобалы захватываются при первом импорте библиотеки — до того, как любая спека успеет
поставить фейки, — и всё, что после теста осталось `undefined`, возвращается на место. Только это:
значение, которое спека подменила намеренно, на месте и остаётся нетронутым, так что починка не может
затереть сознательную заглушку.

```ts
import { getWatchedTimerGlobals, restoreTimerGlobals } from 'vitest-auto-spy/setup';

restoreTimerGlobals(); // безопасно в любой момент и сколько угодно раз
getWatchedTimerGlobals(); // имена, захваченные в этой среде
```

`setupFakeTimers()` делает ту же починку в своём собственном `afterEach`, так что сюита, которая им
пользуется, прикрыта независимо от того, установлен ли `setupAutoSpy()`.

## 7. Как назвать файл, запечатавший глобал {#_7-naming-the-file-that-sealed-a-global}

По желанию, и отвечает на вопрос, на который иначе отвечают грепом по репозиторию.

`Object.defineProperty(document, 'cookie', { value, writable: true })` — это способ времён Jest
заглушить браузерный глобал, а `configurable` по умолчанию `false`. При пофайловой изоляции это
безвредно: среда всё равно выбрасывается. При `isolate: false` свойство больше нельзя ни
переопределить, _ни_ удалить, так что каждый следующий файл в этом воркере его наследует, и падает
какая-нибудь библиотека, через прогон, и ничто не показывает на файл, который это сделал.

```ts
setupAutoSpy({ guardGlobals: 'throw' }); // или 'warn', пока большую сюиту разгребают
```

```text
[vitest-auto-spy] "app info > reads the cookie" (src/app/diagnostics/app-info.spec.ts) redefined document.cookie as non-configurable (Object.defineProperty defaults configurable to false), so no later file can put it back.
Patch it with mockValueProp(document, 'cookie', value) instead: it records what it replaced and undoes it after the test.
Docs: https://asdalexey.github.io/vitest-auto-spy/utilities/setup#_7-naming-the-file-that-sealed-a-global
```

Помощник, которого называет отчёт, выбирается по найденному дескриптору: значение —
`mockValueProp`, геттер — `mockReadonlyPropGetter`, геттер с сеттером — `mockAccessorsProp`.

`globalThis`, `document` и `navigator` сравниваются до и после каждого теста; в отчёт попадают только
свойства, которые появились _и_ не могут быть убраны. `guardGlobalPatches(reaction)` экспортируется
для сюиты, которой нужен более узкий охват.

## 8. Как падать на реджекте, который проглотил zone.js {#_8-failing-on-a-rejection-zone-js-swallowed}

По желанию, и это единственный переключатель на этой странице, который меняет, говорит ли зелёный
прогон правду.

zone.js подменяет глобальный `Promise`. Отклонённый `ZoneAwarePromise`, который никто не обработал,
вычерпывается в `api.microtaskDrainDone()` и сообщается через `api.onUnhandledError` — а это
`console.error` и больше ничего. Он никогда не доходит до `process.on('unhandledRejection')`, канала,
который слушает Vitest, так что раннеру ничего не сообщают: файл, отклонивший сотню промисов, всё
равно выходит с 0.

Прячет это обычный код:

```ts
it('renders once compiled', () => {
  TestBed.compileComponents().then(() => expect(component.ready).toBe(true)); // никогда не выполнится
});
```

Тест кончается раньше, чем выполнится колбэк, так что ассерт срабатывает уже после того, как
принадлежащий ему тест отчитался зелёным, — а когда он падает, это падение _и есть_ реджект, который
никто не обработал. То же касается `async`-хелпера, вызванного без `await`, и `TypeError`, брошенного
внутри `import('…').then(…)` в продакшн-коде. В одном переехавшем Angular-монорепозитории — 1688
файлов спек, 11 587 тестов, зелено, код возврата 0 — ровно за этим сидели шесть настоящих дефектов,
два из них — просто ложные ассерты.

```ts
setupAutoSpy({ strayRejections: true });
```

Реджект тогда роняет тот тест, в котором раннер находился, когда zone.js на него махнул рукой:

```text
[vitest-auto-spy] 1 promise rejection went unhandled in "TaskListComponent > renders once compiled", and zone.js swallowed it into console.error:
  - AssertionError: expected false to be true
An assertion that settles after its test has ended cannot fail it, so that test passed without it. Return or await the promise — `.then(() => expect(…))` or an async helper called without await is the usual cause.
```

«Went unhandled in» называет тест, в котором реджект всплыл, а не тот, что его создал, потому что
реджект, созданный тестом одного файла, регулярно всплывает во время другого; если тестов несколько,
каждая строка говорит, где всплыл её реджект. Завершающий совет меняется в зависимости от вида —
упавший матчер и брошенная ошибка — разные баги.

Два сознательных ограничения. zone.js должен быть уже загружен: этот пакет его никогда не
импортирует — zoneless-проект не должен его тянуть, — так что `import 'zone.js';` в начале
setup-файла или собственная точка входа билдера `@angular/build:unit-test` — вот что его туда кладёт,
и без него вызов **бросает**, а не молча наблюдает за пустотой. И слушатель
`process.on('unhandledRejection')` не ставится: собственный обработчик Vitest выключается, как только
появляется второй слушатель, так что добавить его — значит _заглушить_ нативные реджекты, о которых
раннер и так рапортует и из-за которых роняет прогоны. Нативные реджекты — не пробел; пробел —
проглоченные зоной.

Части экспортируются для сюиты, которой нужен более узкий охват или нужны сами захваты:

```ts
import { countStrayRejections, flushStrayRejections, trackStrayRejections } from 'vitest-auto-spy/setup';

const stop = trackStrayRejections(); // идемпотентно; возвращает отмену, восстанавливающую прежний обработчик

afterEach(() => {
  const stray = flushStrayRejections(); // { reason, assertion, testName }[], и начинает копить заново

  expect(stray).toEqual([]);
});
```

Каждая принимает тот же необязательный host, что и трекеры забытых таймеров, по умолчанию — настоящие
глобалы. `countStrayRejections()` бросает, когда за хостом никто не следит: просить счёт, который
всегда `0`, потому что никто не наблюдает, — тот режим отказа, о котором стоит кричать, — а
`flushStrayRejections()` вместо этого возвращает пустой массив, так что забытая после выключения опции
уборка не роняет сюиту.

Реджект, который раннер **уже** вменил завершившемуся тесту, второй раз не рапортуется. `async`-тест,
проваливший ассерт, оставляет свой `AssertionError` в обоих местах: раннер называет падение, и та же
ошибка приходит сюда как никем не обработанный реджект. Красный прогон из-за этого печатал два
сообщения на каждое падение, а первое, что делает читатель со вторым, — идёт искать дефект, которого
нет. Что переживает фильтр — то, ради чего проверка и существует: реджекты, не роняющие вообще ни
одного теста.

Правило [`no-floating-assertion`](/ru/utilities/eslint-plugin) ловит самую частую форму статически,
ещё до того, как она вообще выполнится.

## 9. Подрезка реестра моков, который никто не опустошает {#_9-pruning-the-mock-registry-nothing-empties}

По желанию, и это единственный переключатель на этой странице, который про то, во что прогон
обходится, а не про то, что он рапортует.

`vi.fn()` и `vi.spyOn()` добавляют созданный мок в единственный `Set` уровня модуля внутри
`@vitest/spy`, потому что именно его обходит `vi.clearAllMocks()`, — и ни один API оттуда ничего
никогда не убирает. При `isolate: true` модуль переоценивается на каждый файл, и множество каждый раз
начинается пустым. При `isolate: false` он оценивается один раз на воркер и только растёт, и большая
сюита чувствует обе половины этого:

- `clearMocks: true` обходит каждый мок каждого уже прогнанного файла **перед каждым отдельным
  тестом**, так что цена очистки растёт вместе с числом уже пройденных тестов;
- куча воркера держит сразу все моки прогона — с записанными аргументами, а через них и с целыми
  деревьями компонентов.

```ts
setupAutoSpy({ pruneMockRegistry: true }); // оставить только моки, переживающие файл
```

Для этого множества нет API, поэтому оно берётся у единственного, кто его обходит: `Set.forEach`
передаёт множество в колбэк третьим аргументом, так что `vi.clearAllMocks()` под ненадолго
пропатченным `Set.prototype.forEach` отдаёт реестр наружу. Захват проверяется по пробному моку, и без
совпадения ничего не подрезается — медленный прогон лучше сломанного.

Половина, которую стоит понять до включения, — что **не должно** уйти. Выбросить мок из реестра значит,
что `vi.clearAllMocks()` и `clearMocks: true` его больше не видят, а значит, его вызовы копятся молча:
безвредно для мока, умирающего вместе с создавшим его файлом, и баг для `vi.fn()` уровня модуля в
общем `*.mock.ts`, который импортируют шесть файлов спек. Первый импортировавший его файл его создаёт,
наивная подрезка выбрасывает его в конце этого файла, а файл, который окажется **вторым**, падает на
вызовах, сделанных его предшественником, — и читается это как флакающие тесты, потому что кто пойдёт
первым, решает раннер.

Поэтому граница проведена там, где она наблюдаема: всё, что уже лежит в реестре к моменту старта
хуков файла, было создано во время вычисления графа модулей, а это ровно и значит «живёт в модуле», —
и оно остаётся; всё добавленное после принадлежит тесту или хуку этого файла и уходит вместе с файлом.
Один случай попадает не на ту сторону линии — модуль, впервые загруженный динамическим `import()`
внутри теста, — и об этом говорится прямо:

```ts
// fixtures/navigation.mock.ts — импортируется шестью файлами спек
export const navigation = { setFocus: keepMockRegistered(vi.fn()) };
```

### Моки, которые он оставляет, он же и охраняет {#the-mocks-it-keeps-it-also-guards}

У того, что мок остаётся зарегистрированным, есть второе следствие, и как раз на его поиски уходит
день. `clearAllMocks` — не единственный, кто обходит это множество: `vi.resetAllMocks()` обходит то же
самое и вызывает `mockReset()` на всём подряд, а `mockReset` возвращает реализацию на место, только
если её передали в `vi.fn(implementation)`. `vi.fn()`, получивший поведение через цепочку
`.mockReturnValue(…)` или `.mockReturnThis()`, просто остаётся отвечать `undefined` — в `@vitest/spy`
это записано как `resetToMockImplementation ? mockImplementation : undefined`.

При `isolate: false` счёт приходит совсем в другом месте. Одна спека вызывает `vi.resetAllMocks()` в
своём `afterEach`; **другой** файл позже в том же воркере умирает внутри прикладного кода, потому что
общий дубль, которого он вообще не трогал, теперь отвечает `undefined`:

```
TypeError: Cannot read properties of undefined (reading 'info')
  app.component.ts:316   AppComponent.syncAllProcesses
```

Ничто в этом падении не называет спеку, которая его вызвала. Оно переезжает всякий раз, когда раннер
переупорядочивает файлы, оно никогда не воспроизводится на файле, который делал сброс, а
`vi.restoreAllMocks()` — вызов, которым естественно пробуют, — не вызывает его вовсе: в Vitest 4 он
обходит `MOCK_RESTORE`, куда пишет только `vi.spyOn`, так что проба, построенная вокруг него,
возвращается зелёной и уводит поиск не туда.

Поэтому реализация, которую долгоживущий мок несёт в момент первой классификации, запоминается и
возвращается перед тестом, который её потерял. Только когда она потеряна: мок, который тест намеренно
переопределил, остаётся таким, каким тест его оставил, а мок, у которого реализации не было никогда,
не трогается вообще. Хук — `beforeEach`, потому что Vitest применяет `restoreMocks` / `mockReset` /
`clearMocks` из `onBeforeTryTask`, который выполняется _до_ хуков `beforeEach`, а не после них.

Части экспортируются для сюиты, которой нужны они без остального: `trackMockRegistry()` ставит те же
хуки сам по себе, `keepRegisteredMocks()` помечает всё зарегистрированное на текущий момент как
долгоживущее, `pruneMockRegistry()` — разовое подметание, возвращающее, сколько ушло,
`restoreLongLivedImplementations()` — та самая починка, возвращающая, сколько вернула на место, а
`getMockRegistrySize()` рапортует, сколько осталось, — `undefined`, когда захват не состоялся.

## 10. Строгие дубли на всю сюиту {#_10-strict-doubles-for-the-whole-suite}

По желанию, и это единственный переключатель здесь, который меняет поведение **дубля**, а не среды.

```ts
setupAutoSpy({ strict: true }); // метод, который никто не настроил, бросает и называет себя
```

Выключено — метод, который никто не настроил, возвращает `undefined`, легальное значение, так что
падение всплывает там, где оно в конце концов используется, за несколько фреймов от пропуска и обычно
внутри продакшн-кода. Включено — он бросает на том вызове, который его породил, называя класс, метод и
аргументы. Форма для отдельного дубля — `createSpyFromClass(X, { strict: true })`; здесь тот же
переключатель на всю сюиту, так что переход на него — одна строка, а не правка на каждый вызов фабрики.

`onUnstubbedCall` — общая форма: что бы она ни вернула, это становится результатом вызова, так что
сюита может сначала записать пробелы, а потом уже включить бросание:

```ts
setupAutoSpy({ onUnstubbedCall: ({ className, method }) => console.warn(`unstubbed ${className}.${method}`) });
```

Собственная конфигурация дубля побеждает, **включая явный `strict: false`**, — и это единственный
способ вывести одного широкого коллаборатора из-под умолчания.

Две вещи про время жизни, обе важны при `isolate: false`. Умолчание взводится, только когда реально
передана одна из двух опций, так что голый `setupAutoSpy()` не может затереть умолчание, поставленное
чем-то другим. И оно снимается в `afterAll` того файла, который его взвёл: модуль, который его хранит,
общий для всех файлов воркера, так что оставленное взведённым умолчание уронило бы спеку, которая ни на
что не подписывалась, сообщением про класс, к которому эта спека не имеет отношения. Это тот же шов,
которым пользуется `strayTimers`, и по той же причине.

**До этого релиза переключатель не доходил ни до одного дубля, построенного спекой.** Умолчание жило в
переменной модуля, а `setupAutoSpy` поставляется в `/setup`, тогда как фабрики — в `dist/index.js` и
`dist/angular.js`, и у каждого своя копия этого модуля: setup-файл взводил одну копию, а спеки читали
другую. Angular-потребитель на 1759 файлов прогнал полный набор из 12 717 тестов со `strict: true`, и
все остались зелёными. Умолчание, вместе с ним `onUnstubbedCall` и `setSpyEngine()`, теперь живут на
`globalThis`, где каждый бандл читает один и тот же ответ.

Ненастроенный геттер строгого дубля по-прежнему отвечает `undefined`, а observable-свойство, которое
никто не накормил, ничего не присылает: бросок на чтении сломал бы дифф упавшей проверки, в котором
печатается дубль. `unconfiguredReads` сообщает о них после теста, а `onUnstubbedRead` сначала их
обследует:

```ts
setupAutoSpy({ strict: true, unconfiguredReads: 'warn' }); // по умолчанию 'off'; 'throw' роняет тест
```

Докуда дотягивается охрана, что считается настроенным, вся цепочка приоритетов и что считает отчёт о
чтениях — на странице [Строгий режим](/ru/core/strict-mode#reads-nobody-configured).

## 11. Бюджет хуков, которого у Jest был всего один {#_11-the-hook-budget-jest-had-only-one-of}

Включено по умолчанию, потому что оно только дописывает предложение к уже упавшему тесту.

```ts
setupAutoSpy({ hookTimeoutHint: false }); // выключить
```

Jest разрешает **один** бюджет и тратит его одинаково на хук и на тело теста:

```js
const timeout = hook.timeout || getState().testTimeout; // jest-circus
const timeout = test.timeout || getState().testTimeout;
```

Vitest разрешает два, и у `hookTimeout` собственное умолчание — 10 000 мс. Так что сюита, перенёсшая
из Jest-пресета `testTimeout: 30000` в конфиг раннера и на этом остановившаяся, даёт каждому хуку
треть того бюджета, что получают её тесты, и никто об этом не говорит.

Дорого это из-за того, куда прилетает падение. Vitest приписывает таймаут `beforeEach` **тесту**, и
длительность теста при этом равна лимиту:

```text
× should create 10045ms
```

Читается как медленный тест. Тело, которое он называет, не выполнялось вовсе, так что эти десять секунд
внутри него не найти, и читатель проводит вечер не в том файле. Подсказка вешает недостающее
предложение на саму ошибку, называя оба бюджета и поле, которое надо задать:

```text
[vitest-auto-spy] hookTimeout is 10000ms while testTimeout is 30000ms, so this hook ran on a smaller budget than the test body it prepares — Vitest resolves `hookTimeout` on its own and defaults it to 10000ms. Set `hookTimeout` next to `testTimeout` in the runner config.
Docs: https://asdalexey.github.io/vitest-auto-spy/utilities/setup#_11-the-hook-budget-jest-had-only-one-of
```

Она молчит, когда бюджеты совпадают, — тогда хук действительно медленный и конфиг ни при чём, — и
молчит для хука, назвавшего собственный лимит (`beforeEach(fn, 300)`). До `beforeAll` не дотянуться по
построению: его таймаут рапортуется как упавшая _сюита_, все тесты помечаются пропущенными, и ни один
`afterEach` не выполняется, чтобы подсказка на нём проехала.

Перенося конфиг раннера с Jest, поставьте оба рядом и считайте единственное число Jest относящимся к
обоим:

```ts
test: {
  testTimeout: 30_000,
  // У Jest на оба был один бюджет; Vitest сам по себе ставит здесь 10_000.
  hookTimeout: 30_000,
}
```

Одно соседнее поле расходится тихо и меняет только отчёт: `slowTestThreshold` — это `5` в Jest
(**секунды**) и `300` в Vitest (**миллисекунды**), так что переехавшая сюита начинает помечать
медленными бо́льшую часть своих файлов. Смена единиц, а не регрессия.

## 12. Таймаут, который объясняют часы, а не код {#_12-a-timeout-the-clock-explains-not-the-code}

Включено по умолчанию и молчит, пока часы не заморожены с колбэками в очереди.

```ts
setupAutoSpy({ frozenClockHint: false }); // выключить
```

Фейковые таймеры превращают ожидание в ожидание навсегда. `await new Promise((r) => setTimeout(r, 10))`
не разрешится никогда, пока что-нибудь их не прокрутит, а всё, что раннер может об этом сказать, — то
же, что он говорит про по-настоящему медленный тест:

```text
Error: Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument …
```

То есть читателю велят поднять бюджет — единственную починку, которая не может сработать, потому что
колбэк не опаздывает, он не выполнится никогда. Под [`globalFakeTimers`](#fake-timers-for-the-whole-run)
всё хуже: в спеке ничто не говорит, что часы фейковые, потому что настройка приехала из Jest-пресета с
`fakeTimers.enableGlobally`, — и таймаут прилетает в файл, где о таймерах вообще ни слова.

Подсказка сообщает `vi.isFakeTimers()` и `vi.getTimerCount()` — часы заморожены, в очереди N колбэков,
и никто их не прокрутил. Это факт, а не догадка, поэтому проверка молчит, когда очередь фейковых часов
пуста: пустая очередь не объясняет никакого таймаута. Подсказка — это один диагноз и одно действие:

```text
[vitest-auto-spy] the clock is frozen and 1 callback is queued on it, so this did not run out of time — nothing advanced the clock, and raising the timeout cannot help. Advance it (`await vi.advanceTimersByTimeAsync(ms)`, `await vi.runAllTimersAsync()`) or use real timers for this test.
Docs: https://asdalexey.github.io/vitest-auto-spy/utilities/setup#_12-a-timeout-the-clock-explains-not-the-code
```

Фраза про `setImmediate` и потерянный HTTP 404, о которых ниже, добавляется, только когда среди
колбэков в очереди есть колбэки `setImmediate`.

**Форма, которая доходит сюда вообще без таймеров на виду, — это HTTP-спека.** `setImmediate` входит в
число глобалов, которые `vi.useFakeTimers()` подменяет по умолчанию, а Express завершает запрос, не
совпавший ни с одним маршрутом, через `finalhandler`, который планирует на `setImmediate`. Поэтому 404
не пишется никогда, и ошибка в маршрутизации рапортуется как тест, занявший тридцать секунд. В таком
файле «тест завис» означает _маршрут не совпал_.

Одного эта подсказка не видит: спеки, чей собственный `afterEach` вызывает `vi.useRealTimers()`. Хуки
идут в обратном порядке регистрации, так что собственный хук спеки выполняется раньше того, который
ставит `setupAutoSpy`, и к моменту чтения часы уже настоящие. Тогда не сообщается ничего — а не
что-нибудь неверное.

## 13. Версия билдера, которая ест память, названная прямо в прогоне {#_13-the-builder-version-that-eats-memory-named-in-the-run}

Включено по умолчанию и молчит, если процесс не является воркером `@angular/build:unit-test` **и**
установленный `@angular/build` не попадает в `[22.1.5, 22.1.7)`.

```ts
setupAutoSpy({ angularBuildHint: false }); // выключить
```

В этом окне билдер собирает бандл юнит-тестов с выключенным code splitting в esbuild, так что каждая
спека — самодостаточный бандл, а `--coverage` растёт на сотни мегабайт без выхода на плато — 791 чанк
/ 596 МБ на сюите из 784 спек, пока прогон не прибьёт OOM killer. Билдер не печатает ни одного
предупреждения, а два места, которые уже об этом говорят — [проверка `doctor`](/ru/utilities/cli#doctor-—-defects-that-never-fail)
`angular-build-splitting-off` и [страница про Angular](/ru/adapters/angular#when-the-unit-test-build-has-code-splitting-off), —
надо ещё найти самому. Эта одна строка печатается изнутри прогона, там, где больно, в stderr, один раз
на воркер: по умолчанию билдер запускает Vitest с `isolate: false`, поэтому `globalThis` в воркере переживает
каждый файл спек, а уведомление держит на нём флаг, который глушит каждое следующее вычисление
setup-файла.

Билдер распознаётся по маркеру, который его собственный setup-файл `vitest-mock-patch` оставляет на
`globalThis` (`Symbol.for('@angular/cli/vitest-mock-patch')`, выставляется до запуска любого
пользовательского setup-файла), так что обычный прогон Vitest ничего не читает вообще. Под билдером
версия берётся из ближайшего `node_modules/@angular/build/package.json` выше рабочей директории — **это
единственное место, где библиотека читает диск**: один файл, только на чтение, через
`process.getBuiltinModule`, а не статическим импортом `node:fs`, чтобы точка входа `/setup` грузилась и
там, где нет `process`, и ничего, кроме этой строки, от находки не зависит. На Node без
`getBuiltinModule` (до 20.16 / 22.3) она молчит, а не гадает.

## 14. Web Storage, которое раннер так и не передал {#_14-web-storage-the-runner-never-handed-over}

Включено по умолчанию, и — как и с глобалами таймеров — оно может только чинить.

Vitest копирует глобалы DOM-среды в `globalThis` через один фильтр:

```js
if (k in global) return KEYS.includes(k);
```

`Storage` в этом списке `KEYS` есть. `localStorage` и `sessionStorage` — нет. Пока Node не клал в
`globalThis` ни того ни другого, первая половина была ложной и оба копировались. Собственный Web
Storage в Node сделал ключ существующим, так что теперь фильтр спрашивает `KEYS`, `KEYS` отвечает
«нет», и хранилище среды не приезжает вовсе. Фильтр работает раньше любого кода, специфичного для
среды, так что jsdom и happy-dom ломаются одинаково:

| Node  | `localStorage` под Vitest   |
| ----- | --------------------------- |
| 24.19 | работает                    |
| 25.9  | `setItem is not a function` |
| 26.7  | `undefined`                 |

Сюита остаётся зелёной при этой поломке, потому что падают только спеки, которые трогают хранилище, —
поэтому обычно она приходит как «CI переехал на новый Node, и умерли одиннадцать никак не связанных
спек».

Починка решает, пользуясь хранилищем, а не глядя на него: она пишет ключ со своим префиксом, читает его
обратно и удаляет. Node 25 предлагает `setItem`, который бросает, Node 26 не предлагает ничего, а
следующий рантайм волен придумать третью форму; хранилище, пережившее круговой рейс, работает, кто бы
его ни реализовал. То, которое не пережило, заменяется — собственным хранилищем окна, где это отдельный
объект, и заменителем на `Map` в остальных случаях.

```ts
import { restoreWebStorage } from 'vitest-auto-spy/setup';

restoreWebStorage(); // безопасно в любой момент и сколько угодно раз
restoreWebStorage({ view: null }); // «окна нет» — не ставит ничего
```

Двух вещей она сознательно не делает. Она ничего не ставит в среде `node`, у которой Web Storage быть и
не должно: выдать проверяемому коду API, которого нет у настоящего рантайма, — изменение большее, чем то,
ради которого она существует. И она оставляет работающее хранилище ровно как есть, так что собственная
заглушка спеки её переживает.

### Хранилище, которое спека ставит себе сама — `stubWebStorage` {#stub-web-storage}

Обратное направление, и не часть `setupAutoSpy()`: спека, которой нужно своё хранилище — пустое или с
данными, и читаемое обратно простой записью, — ставит его из `vitest-auto-spy/dom-stubs`, вместо того
чтобы в каждом проекте писать свой класс `TestingStorage`.

```ts
import { type WebStorageStub, stubWebStorage } from 'vitest-auto-spy/dom-stubs';

let local: WebStorageStub;

beforeEach(() => {
  local = stubWebStorage('localStorage', { items: { token: 'abc' } }); // или 'sessionStorage'
});

it('forgets the token on logout', () => {
  session.logout();

  expect(local.snapshot()).toEqual({});
});
```

`getItem`, `setItem`, `removeItem`, `clear`, `key` и `length` ведут себя как у платформы: ключи и
значения приводятся к строкам, индекс `key()` преобразуется так же, как `unsigned long`. `snapshot()` —
копия, а не представление. Хранилище ставится на `globalThis` — и на `document.defaultView`, если это
отдельный объект, — через `mockValueProp`, поэтому `restoreMockedProps()` возвращает то, что было до
него, включая починенное хранилище; ставьте его в `beforeEach`, как и любую другую заглушку.

Где они намеренно расходятся: починка не трогает работающее хранилище и ничего не ставит в среде
`node`, потому что угадывает, какой должна была быть среда; заглушка заменяет то, что есть, и ставится
и в среде `node`, потому что её попросила спека. Чего она не делает: доступ по имени свойства
(`localStorage.token`, `Object.keys(localStorage)`) записей не видит, событие `storage` не
срабатывает, квоты нет.

## 15. Ключ на `Object.prototype`, из-за которого прогон перестаёт собирать тесты {#_15-the-key-on-object-prototype-that-stops-the-run-collecting}

Включено по умолчанию, и это единственная проверка на этой странице, без которой прогон отчитывается
успехом о коде, который не выполнялся.

Vitest собирает хуки файла в `mergeHooks`, а тот обходит объект хуков через `for…in`. Одно own
enumerable свойство на `Object.prototype` даёт лишний ключ, который спредится как массив, — и
происходит это на стадии **collect**:

```text
TypeError: Spread syntax requires ...iterable[Symbol.iterator] to be a function
```

без стека: `parseErrorStacktrace` фильтрует кадры по `stackIgnorePatterns`, куда входят
`"/vitest/dist/"` и `/\/@vitest\/\w+\/dist\//`, а все кадры этой ошибки лежат именно там. При
`isolate: false` ключ переживает файл, который его записал, поэтому жертвы — весь хвост этого
воркера. На наборе из 1759 файлов отчёт читался как `145 failed | 1613 passed` и строкой ниже
`11880 passed | 0 failed` — упавших тестов ноль, потому что эти 145 файлов не выполнялись, — а число
гуляло между 0, 75, 83, 122, 135, 145 и 155 на неизменном дереве, потому что прогон, в котором
виновник отработал последним, выходил зелёным. Так же ломается всё, что обходит plain object: вторым
местом всплыла таблица mime в `superagent` (`typeMap[type].map is not a function`).

```ts
setupAutoSpy(); // prototypePollution: 'throw' — передайте 'warn', чтобы снимать ключ и только сообщать
```

```text
[vitest-auto-spy] "CheckoutOpenService > closes on destroy" (src/app/checkout/checkout-open.service.spec.ts) left "ngOnDestroy" (a function) on Object.prototype as an enumerable property.
It has been taken off: left there, it stops every later spec file in this worker from collecting. Define it on the prototype of the class it belongs to, or with enumerable: false.
Docs: https://asdalexey.github.io/vitest-auto-spy/utilities/setup#_15-the-key-on-object-prototype-that-stops-the-run-collecting
```

Отчёт называет тест и вид значения (`(a number)`, `(a function)`), а путь даёт от корня проекта; на
проверке в конце файла, когда тест не идёт, он называет только файл.

Запись почти всегда случайна. Код, который декорирует класс через
`Object.getPrototypeOf(instance)`, получает сам `Object.prototype`, как только `instance` — это
объектный литерал из `useValue`-провайдера или тест-дубль:

```ts
const proto = Object.getPrototypeOf(instance); // для plain object это Object.prototype
proto['ngOnDestroy'] = function () { … };      // теперь свойство есть у каждого объекта
```

Патчить нужно прототип класса, откуда пришёл объект, и никогда — прототип plain object или дубля.
`Object.prototype`, `Array.prototype` и `Function.prototype` сравниваются до и после каждого теста;
ключ, который среда несла изначально, не трогается, так что собственный полифил проекта не сметётся.
Базовая линия файла снимается в его `beforeAll` и сверяется ещё раз в очистке этого хука, которая
выполняется после каждого `afterAll`, — поэтому ключ, записанный в собственном `beforeAll` или
`afterAll` файла, тоже попадает в отчёт, а не считается тем, с чем среда пришла.
`guardPrototypePollution(reaction)` — та же проверка, зарегистрированная отдельно, для набора,
которому она нужна точечно; оба конца файла она покрывает так же.

### Ключ, который оставил предыдущий файл {#the-key-an-earlier-file-left-behind}

Ключ, записанный во время _импорта_ файла спеки, во время его сбора (collect) или в `afterAll`,
недосягаем для хуков этого файла — а погибает за него **следующий**, на collect, раньше, чем
что-либо успеет отчитаться. Единственный шов — setup-файл: раннер выполняет его перед collect
каждого файла. Поэтому `setupAutoSpy()` сравнивает три прототипа с тем, с чем воркер вошёл в файл,
снимает всё, что оставил предыдущий, и пишет отчёт в stderr:

```text
[vitest-auto-spy] "ngOnDestroy" was left on Object.prototype by the previous spec file of this worker, src/app/checkout/checkout-open.service.spec.ts — while it was imported, collected or in an afterAll — and has been taken off.
Left on, the key stops every later spec file in the worker from collecting: Vitest walks a file's hooks with for…in. In that file, patch the prototype of the class an object came from, never Object.getPrototypeOf(someObjectLiteral).
```

Файл — тот, чей setup выполнился в этом воркере перед текущим; его запоминают в начале каждого
файла.

Это никогда не бросает исключение, каким бы ни был grade: файл, который должен был упасть, — не тот,
который записал ключ. Stderr вместо `console.warn` — по той же причине, по которой туда пишет
свёртка stray-таймеров: задачи ещё нет, а перехваченный вывод, никому не принадлежащий, Vitest
выбрасывает. Ключ, который не даёт себя удалить, вместо этого принимается в базовую линию, чтобы
остаток воркера не слышал о нём по разу на файл.

Это та половина, которую `guardPrototypePollution(reaction)` заменить не может. Он регистрируется
из самого файла спеки, и к моменту его загрузки вред от предыдущего файла уже сидит в его базовой
линии, а collect, который собирался упасть, — его собственный. На шве между двумя файлами стоит
только вызов из **setup-файла**, поэтому эта половина и принадлежит `setupAutoSpy()`.

## 16. Вывод в консоль, который никто не поглотил {#_16-console-output-nothing-absorbed}

Включается явно. Вывод в консоль из зелёного теста — это либо дефект, на который тест не проверил, либо
шум, под которым потеряется следующий настоящий провал, а раннер одно от другого не отличает: он
приписывает строку тесту и идёт дальше.

```ts
setupAutoSpy({ strayConsole: 'throw' });
```

Любой вызов пишущего метода консоли во время теста, который ничто не поглотило, роняет **этот тест**
по имени:

```text
[vitest-auto-spy] "CartService > reports a failed load" wrote to console.error 1 time and nothing absorbed it:
  - console.error: Error: load failed {"id":7}
      at CartService.load (src/app/cart.service.ts:41:15)
Absorb what the test expects — installConsoleSpies() in a beforeEach, then assert consoleErrorSpy — or fix the code if the output is a defect.
```

Отчёт цитирует метод, первые три строки написанного (по 200 символов, пять вызовов, дальше
`… and N more`) и первый кадр стека вне `node_modules` — а для строки, которую написала зависимость,
прямого вызывающего, то есть имя пакета. Обрезанная строка не теряет ссылку: первый URL, который
отрезало, дописывается к цитате. Vitest принимает кадр за место ошибки, так что его code frame
указывает прямо на `console.error`.

Совет называет спай для каждого метода, который писал, — `consoleErrorSpy`, `consoleWarnSpy`, … — и
тихий `vi.spyOn` для метода, у которого в `/console` спая нет (`table`, `dir`, …). Если вывод прошёл
через `vi.spyOn(console, 'error')` без реализации, отчёт говорит ровно это:
`vi.spyOn(console, 'error') calls through — add .mockImplementation(() => undefined).`

**Что считается.** `log`, `info`, `warn`, `error`, `debug`, `trace`, `table`, `dir`, `dirxml`,
`timeLog`, `timeEnd`, `count`; `group` / `groupCollapsed` — только с меткой; `assert` — только при
ложном условии. `time`, `groupEnd` и `countReset` ничего не пишут и не отслеживаются.

**Что поглощает.** Охрана кладёт записывающую обёртку _под_ то, что стоит на `console`, так что вызов
посторонний ровно тогда, когда до неё дошёл:

| В тесте                                                              | Итог                                                        |
| -------------------------------------------------------------------- | ----------------------------------------------------------- |
| `installConsoleSpies()` из `vitest-auto-spy/console`, затем проверка | поглощено — спай не вызывает оригинал                       |
| `vi.spyOn(console, 'error').mockImplementation(() => undefined)`     | поглощено                                                   |
| `vi.spyOn(console, 'error')` без реализации                          | **посторонний** — записывает вызов, потом вызывает оригинал |
| ничего                                                               | **посторонний**                                             |

**Вне тестов.** Вывод во время импорта файла, в `beforeAll` / `afterAll`, из колбэка, сработавшего
после конца своего теста, или в тесте, чей `afterEach` так и не выполнился, роняет **файл** в
`afterAll`. Отчёт называет момент — `while the file was being imported`, `in a beforeAll`,
`after a test had ended` — и даёт совет только для него; если моменты смешаны, помечена каждая
строка. `beforeAll` вложенного `describe`, который выполняется после более ранних тестов, тоже
назван `beforeAll`. Лог сторонней библиотеки при импорте ловится так же и приписывается тому файлу,
который этот импорт вызвал, — при `isolate: false` это первый файл воркера, импортировавший модуль,
поэтому от прогона к прогону отчёт может переезжать с файла на файл.

Для вывода, написанного во время вычисления модуля, совет — это диагноз и исправление: какой модуль
вычислялся, какую строку менять, и одна оговорка про `isolate: false`. Только когда эта строка лежит
в `node_modules` — в коде, которым набор не владеет, — отчёт предлагает `strayConsole: { allow: [...] }`.

**Назван тот файл, который писал.** Каждый вызов запоминает spec-файл, в котором был сделан, а
отчёт конца файла выполняется в том же подметании, что и отчёты о забытых таймерах и слушателях, так
что отчёт, бросивший раньше, не может вытолкнуть его в следующий файл. Если конец файла не выполнился
вовсе — бросил его собственный `afterAll`, — следующий файл сообщит о выводе под именем того файла,
который писал, и скажет об этом.

**Знакомая строка объясняется.** Причина читается из всего вывода, а не из процитированных 200
символов, и печатается под `Likely cause:` — для `NG0912` названы оба класса и селектор и сказано,
что это две копии одного компонента в одном бандле; для любого другого `NGxxxx` Angular — ссылка,
которую он сам напечатал:

```text
[vitest-auto-spy] src/app/checkout.component.spec.ts wrote to console.warn 1 time while the file was being imported and nothing absorbed it:
  - console.warn: NG0912: Component ID generation collision detected. Components 'UiRadioGroupComponent' and '_UiRadioGroupComponent' … https://angular.dev/errors/NG0912
      at Function.<static_initializer> (src/app/ui/radio-group.component.ts:210:44)
Likely cause:
  - Angular gave `UiRadioGroupComponent` and `_UiRadioGroupComponent` (selector `ui-radio-group`) one component id, so the bundle holds two copies of one component — … Import the component from one place.
Written while src/app/ui/radio-group.component.ts was evaluated, before any hook — no spy can absorb it; fix it at src/app/ui/radio-group.component.ts:210:44. Under isolate: false it is reported on the first file of the worker that imports that module.
```

**То, что поставил тест, его не переживает.** Каждый метод консоли, подменённый тестом, возвращается
после теста; подменённый файлом — в теле `describe`, в `beforeAll` или в начале модуля — после файла.
Под `isolate: false` именно это не пускает тишину одного файла в следующий.

**Импорт `/console` под охраной ничего не ставит.** Без охраны импорт `vitest-auto-spy/console`
сажает на консоль тихие спаи. Под `isolate: false` этот импорт выполняется **один раз на воркер**,
так что спаи вставали в том файле, который импортировал их первым, и оставались во всех следующих —
глуша ровно тот вывод, ради которого охрана существует. Поэтому при включённой охране ставьте их там,
где им место:

```ts
import { consoleErrorSpy, installConsoleSpies } from 'vitest-auto-spy/console';

beforeEach(() => installConsoleSpies()); // на каждый тест — или вызовите в начале файла, на весь файл

it('reports a failed load', () => {
  service.load();

  expect(consoleErrorSpy).toHaveBeenCalledWith('load failed', expect.any(Error));
});
```

Спаи, которые импорт поставил до включения охраны, снимаются в момент включения. Файл, который
импортирует спай, но не ставит его, получает провал выше плюс одну фразу с исправлением.

**Собственные предупреждения библиотеки — тоже вывод в консоль.** `console.warn` от `injectSpy` или от
конфигурации `createSpyFromClass` роняет тест, который его вызвал, так что охрана превращает каждое
предупреждение в провал; [`misconfiguration: 'throw'`](#misconfiguration-reports-that-fail-at-the-call)
заставляет те же отчёты бросать прямо в месте вызова, и стек там лучше.

**Больше она ничего не меняет.** Обёртка передаёт каждый вызов дальше без изменений, так что
атрибуция `stdout | file > test` у Vitest, `onConsoleLog` и вывод упавшего теста остаются ровно
такими же — проверено на Vitest 4.1 и 5.0, с охраной в setup-файле и двумя spec-файлами в одном воркере при `isolate: false`.

**`allow` — последнее средство**, для шума окружения, до которого не дотянется ни одна спека, и
никогда — для вывода кода под тестом: это дефект, который надо чинить, или проверка, которую надо
написать:

```ts
setupAutoSpy({ strayConsole: { allow: ['Download the React DevTools', /^Lit is in dev mode/] } });
```

Строка совпадает как подстрока, `RegExp` ищется поиском (флаги `g` / `y` ни на что не влияют).
`reaction` у объектной формы по умолчанию `'throw'`; `'warn'` печатает тот же отчёт — в консоль для
теста, в stderr для файла — не роняя его: так большую сюиту сначала меряют, а потом включают.
`guardStrayConsole(reaction)` — та же охрана, зарегистрированная сама по себе.

Чего она не видит: вывода напрямую в `process.stdout` / `process.stderr` и собственной виртуальной
консоли jsdom, которая захватила настоящую консоль раньше, чем её перехватил Vitest, — перенаправьте
события `jsdomError` в `console.error`, если они должны считаться. Реджект, проглоченный zone.js,
печатается через `console.error`, так что при включённом `strayRejections` о нём сообщают дважды;
побеждает первый провал, и это отчёт о реджекте.

## 17. Атрибут, оставленный на общем документе {#_17-an-attribute-left-on-the-shared-document}

Включается явно и входит в `preset: 'strict'`. При `isolate: false` все spec-файлы воркера рендерят в
один документ jsdom, и между файлами его никто не возвращает. В сюите, где это нашли, эффект
компонента клавиатуры делал `renderer.setAttribute(document.body, 'data-reset-focus', '')`, и никто
атрибут не снимал. Сервис навигации в другом месте выходит раньше, если
`document.querySelector('[data-reset-focus]')` что-то находит, — и его спека роняла 34 теста из 209:
примерно в одном полном прогоне из шести, только когда два файла попадали в один воркер, и никогда
сама по себе. Остальные охраны этого не видят: ничего не запечатано и не добавлено на прототип, не
осталось ни таймера, ни вывода в консоль, ни реджекта.

```ts
setupAutoSpy({ documentPollution: 'throw' }); // 'warn' возвращает документ и только сообщает
```

```text
[vitest-auto-spy] "KeyboardComponent > renders the layout" (libs/keyboard/src/lib/keyboard.component.spec.ts) left the shared document changed:
  - <body> data-reset-focus="" added
It has been put back, because every later spec file in this worker shares this document. Undo it in the teardown of what set it: ngOnDestroy / DestroyRef.onDestroy of the component, or destroy the fixture that owns it.
Docs: https://asdalexey.github.io/vitest-auto-spy/utilities/setup#_17-an-attribute-left-on-the-shared-document
```

Совет про teardown Angular печатается, только когда Angular обнаружен (`globalThis.ng` или маркер
CDK / `ng-` в самом изменении); иначе совет — `afterEach`, а для изменения вне тестов — `afterAll`.

Атрибуты `<html>`, `<head>` и `<body>` записываются перед каждым тестом. Каждый атрибут, добавленный,
изменённый или удалённый к его концу, попадает в отчёт с обоими значениями и возвращается на место, а
тест падает. Изменение, сделанное в `beforeAll` и не отменённое, вменяется файлу.

Пустой `class` или `style` считается отсутствующим атрибутом. `classList.add`, а затем `classList.remove`
(или `addClass` / `removeClass` у Renderer2, или стиль, выставленный и сброшенный) оставляют `class=""` там,
где атрибута не было, и отличить одно от другого нечем — на одном потребительском наборе этот артефакт дал
9 из первых 32 падений. Любое другое пустое значение по-прежнему изменение: `data-reset-focus=""` — это
флаг, который матчит `[data-reset-focus]`.

**Важно, когда смотреть.** Не в `afterEach`: сетап-файл регистрирует свои хуки после хуков TestBed
(`init-testbed.js` у билдера — первый сетап-файл), `afterEach` выполняются в обратном порядке, и
проверка в `afterEach` шла бы раньше, чем TestBed уничтожит фикстуры, — сообщая о каждом атрибуте,
который компонент снимает при уничтожении, и о каждом `<style>` и корневом элементе, которые Angular
убирает в той же уборке. Проверка теста выполняется из `onTestFinished`, который Vitest вызывает после
всей цепочки `afterEach`; проверка файла — из очистки `beforeAll`, которая идёт после всех `afterAll`.
Замерено на zoneless TestBed: в момент `afterEach` стиль компонента, его корневой элемент и атрибут,
принадлежащий его `DestroyRef`, были на месте, а в обеих этих точках — уже нет.

| Форма                                                         | Что делает                                                                                 |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `'throw'` / `'warn'` / `'off'`                                | Реакция; `'warn'` пишет в stderr и всё равно возвращает документ                           |
| `{ reaction }`                                                | То же, по умолчанию `'throw'`                                                              |
| `{ nodes: true }`                                             | Ещё и дочерние элементы `<head>` и `<body>`: добавленные убираются, удалённые возвращаются |
| `{ ignoreAttributes: ['aria-hidden', /^data-cdk-/] }`         | Атрибуты, которые не трогать, — точным именем или RegExp                                   |
| `{ nodes: true, ignoreNodes: 'style, link[rel=stylesheet]' }` | Дочерние элементы, которые не трогать, — CSS-селектором                                    |

`nodes` по умолчанию выключен: библиотека, которая вставляет стили при первом импорте, делает это раз
на воркер, и проверка узлов вменила бы этот `<style>` тому тесту, который импортировал её первым.
Стоимость: около 4 мкс на тест для атрибутов, 10 мкс с `nodes`.

Чего она не видит:

- записи, сделанной, пока spec-файл **импортируется**, — она случается раньше любого хука этого файла,
  так что уже входит в исходный снимок;
- фикстуры, оставленной в живых через `teardown: { destroyAfterEach: false }`: TestBed уничтожает её в
  `beforeEach` следующего теста, так что принадлежащее компоненту вменяется тесту, который его
  отрендерил, — включите `destroyAfterEach` или внесите этот атрибут в `ignoreAttributes`;
- `document.title`, фокуса, cookie и `customElements` — это не атрибуты, а определение кастомного
  элемента отменить нельзя.

Что ожидать в Angular-сюите: код, который украшает `<html>` или `<body>` на всё приложение, —
блокировку прокрутки от оставленных открытыми модалки или оверлея, класс темы или платформы,
`lang` / `dir` от сервиса i18n, флаг `data-*` от менеджера фокуса. При общем документе каждая из них —
настоящий остаток; чинится закрытием или уничтожением того, что это поставило, а `ignoreAttributes` —
крайнее средство для того, что ставится один раз намеренно.

`guardDocumentPollution(option)` регистрирует ту же проверку отдельно. Только Vitest, как и каждая
охрана из `/setup`: у `bun:test`, `node:test` и Rstest нет сетап-входа, где бы она жила.

## 18. Спай на Web Storage, который раннер не может снять {#_18-a-spy-on-storage-the-runner-cannot-take-off}

Включено по умолчанию. `vi.spyOn(localStorage, 'setItem')` кладёт мок на стораг как собственное
свойство; снятие — это удаление того свойства, чтобы метод прототипа снова показался сквозь него.
Под happy-dom это удаление не срабатывает никогда: его `Storage` раздаёт каждый инстанс,
обёрнутым в Proxy, чей `deleteProperty`-трап знает только stored items. `mockRestore()` — и идущий
тем же путём `vi.restoreAllMocks()` — возвращается зелёным, не сделав ничего. Спай продолжает
записывать, а следующий спек, который делает свой `vi.spyOn`, получает тот же мок с вызовами
предыдущего файла внутри, и «не вызывался» падает через раз.

Ручная отладка ведёт поиск не туда: естественная проба — выполнить `vi.restoreAllMocks()` и
посмотреть, уйдёт ли остаток — это ровно тот путь, который молча не делает ничего, так что она
возвращается зелёной и, кажется, исключает мок.

Записать метод `Storage.prototype` обратно поверх спая через proxy проходит; эта асимметрия —
define проходит, delete нет — и есть починка:

```ts
setupAutoSpy(); // restoreStorageSpies: true — подметание только на границе файла
```

Граница, а не тест — намеренно: спай, поставленный в `beforeAll` на весь файл, во время тестов
этого файла ещё стоит, защищён только следующий файл. `restoreStorageSpies()` — разовый вызов
(возвращает починенные стораги), а `restoreStorageSpies: false` — выключатель для сюиты, которая
намеренно держит спай на методе сторага весь воркер.

Ворота — `vi.isMockFunction`, а не «отличается от прототипа»: jsdom ломает вторую половину того же
контракта, его Storage-прокси превращает `defineProperty` метода в stored item, и ворота,
сравнивающие с прототипом, переписывали бы стораг, который просто странный. Мок чинится; чистый
стораг, намеренная замена и мусорный item jsdom не трогаются.

## 19. Слушатели, переживающие свой файл {#_19-listeners-that-outlive-their-file}

```ts
setupAutoSpy({ strayListeners: true });
```

Слушатель на `window` или `document` — не компонента снимать. Оверлеи, порталы и сервисы
регистрируются на общих целях, и при `isolate: false` всё, что файл оставил зарегистрированным,
срабатывает во время следующего — та же вина не того файла, что у stray-таймера, но без всякой
видимости: ничего не падает, колбэк просто выполняется против моков и DOM, под которые его не
писали.

Разделение — то же, что у подметания реестра моков (раздел 9). `beforeAll` помечает слушателей,
уже стоящих на `window` и `document`, — зарегистрированных при вычислении графа модулей,
одноразовую инициализацию фреймворка, — а `afterAll` снимает всё, добавленное после. Никакого
особого случая для первого файла: регистрации на импорте попадают в базлайн того файла, который их
импортирует, и переживают каждое подметание после этого.

Кусочки экспортированы тоже, из `vitest-auto-spy/setup`:

| Функция                    | Что делает                                                                                    |
| -------------------------- | --------------------------------------------------------------------------------------------- |
| `trackStrayListeners()`    | Обернуть `addEventListener` / `removeEventListener`; идемпотентно, возвращает отмену          |
| `baselineStrayListeners()` | Помечать зарегистрированное сейчас как состояние графа модулей — шаг `beforeAll` по умолчанию |
| `removeStrayListeners()`   | Снять всё, добавленное после базлайна; возвращает, сколько снял                               |
| `countStrayListeners()`    | Сколько осталось; бросает, пока не выполнился `trackStrayListeners()`                         |
| `describeStrayListeners()` | Цель, тип, spec-файл и до пяти кадров каждого — список, который получает `onStrayListeners`   |

`setupAutoSpy({ strayListeners: true, onStrayListeners: ({ removed }) => expect(removed).toBe(0) })`
роняет файл, который протёк, вместо того чтобы молча прибрать за ним; `onStrayListeners: 'throw'`
делает то же отчётом, который называет файл и записывает каждый слушатель как
`keydown on document, added in "<test>" at <frames[0]>`. У слушателя вместо `kind` и `delay` таймера
есть `type` и `target`, а `test` / `outsideTest` те же, поэтому обработчик, печатающий своё
сообщение, печатает эти поля. Под jsdom слушатель,
зарегистрированный с `{ once: true }` и уже сработавший, остаётся в счёте, пока что-нибудь его не
снимет: обёртка не может увидеть срабатывание, не сломав снятие по идентичности из кода под
тестом, а удаление уже сработавшего слушателя и так ничего не делает. happy-dom снимает его через
публичный `removeEventListener`, поэтому там он уходит из счёта сам.

Если включён ещё и `strayTimers`, оба отчёта — обработчик или `'throw'`, всё равно — срабатывают
только после того, как таймеры отменены, а слушатели сняты, так что бросивший `onStrayListeners` не
оставит таймер сработать в следующем файле. Оба срабатывают, даже если первый бросил; два падения —
например, обе опции в `'throw'` у файла, протёкшего и тем и другим, — приходят одним
`AggregateError`, который Vitest показывает как две ошибки. Отчёт конца файла от `strayConsole`
выполняется в том же подметании, поэтому его не пропустят из-за того, что отчёт перед ним бросил.

## 20. Глобалы, возвращаемые на границе файла {#_20-globals-put-back-at-the-file-boundary}

```ts
setupAutoSpy({ restoreGlobals: true });
```

`vi.stubGlobal` откатывает `unstubGlobals`, `vi.spyOn(globalThis, …)` — `restoreMocks`, а простое
присваивание — `global.ResizeObserver = stub` — не отслеживает никто. При `isolate: false` оно
пишется прямо в общий воркер и читается каждым следующим файлом, и падение, которое оно вызывает,
не называет ни свойства, ни файла, который его посадил. `guardGlobals` (раздел 7) покрывает
_непочиняемый_ случай, non-configurable redefine; это сеть для починяемого.

Один снимок на воркер, возвращаемый на каждой границе файла. `captureGlobalBaseline()` делает
его — первый вызов и есть истина воркера, последующий не делает ничего, так что переснятие не
сможет отмыть замену в базлайн, — а `restoreGlobals()` — подметание, возвращающее изменённые
ключи.

Две ловушки, которые восстановление обходит, обе несущие:

- **Дескриптор — не вся история.** DOM-среда ставит свойства окна на `globalThis` парой
  accessor'ов, переправляющих в override-таблицу. `global.ResizeObserver = stub` выполняет сеттер
  и оставляет дескриптор в точности прежним, поэтому восстановление, которое только переопределяет
  дескрипторы, отчитывается «ничего не изменилось», пока заглушка продолжает отвечать. Захваченное
  значение нужно вернуть через тот же сеттер.
- **Собственные обёртки библиотеки должны выжить.** `strayTimers` обёртывает `setTimeout`,
  `strayListeners` — `addEventListener`; восстановление, возвращающее каждый оригинал, снесло бы
  трекинг на первой же границе. Обёртки, которые ставит эта библиотека, несут метку, и
  восстановление её обходит.

Границы сети: ключи, **добавленные** после снимка, никогда не удаляются — фреймворк, ставящий
глобал на импорте, не должен терять его на первой границе, — а глобалы идентичности (`location`,
`document`, `window`, `frames`, `global`, `parent`, `self`, `top`) не возвращаются вовсе:
присваивание им навигирует или подменяет среду, а не заменяет значение. Оставшиеся фейковые часы
снимаются до подметания: восстанавливать дескрипторы таймеров под установленным фейком ломает и
то и другое.

## Одна оценка на всё — `preset: 'strict'` {#one-grade-for-everything-preset-strict}

```ts
setupAutoSpy({ preset: 'strict' });
```

Запускает каждую охрану на самой строгой оценке. Опция, переданная рядом, всё равно побеждает, так
что `{ preset: 'strict', guardGlobals: 'warn' }` ослабляет ровно одну.

| Опция                | При `preset: 'strict'`                        | По умолчанию без него |
| -------------------- | --------------------------------------------- | --------------------- |
| `duplicateCopies`    | `'throw'`                                     | `'throw'`             |
| `propsOutsideHooks`  | `'throw'`                                     | `'warn'`              |
| `guardGlobals`       | `'throw'`                                     | `'off'`               |
| `prototypePollution` | `'throw'`                                     | `'throw'`             |
| `documentPollution`  | `'throw'`                                     | `'off'`               |
| `strayConsole`       | `'throw'`                                     | `'off'`               |
| `misconfiguration`   | `'throw'`                                     | `'warn'`              |
| `strayTimers`        | `true`                                        | `false`               |
| `strayRejections`    | `true`, если загружен zone.js, иначе выключен | `false`               |

`strayRejections` условный, потому что без zone.js он бросает — следить не за чем; пресет проверяет
наличие `Zone.__symbol__`.

Сознательно **не** входит, и у каждого своя причина:

- **`strict`** — строгие дубли меняют то, что _возвращает_ ненастроенный вызов; это решение о том, как
  сюита пишет дубли, а не оценка отчёта. Имя опции было уже занято им, поэтому эта называется `preset`.
- **`unconfiguredReads`** — сторона чтения того же `strict`, значит, и решение то же: она требует
  настроить каждый геттер и поток, которых касается тест, и на существующей сюите начинается с
  обследования (`onUnstubbedRead`), а не с красного прогона.
- **`blockNetwork`** — меняет то, что видит код под тестом.
- **`restoreMocks`** — заодно снимает стабы `vi.spyOn`, поставленные сюитой в `beforeAll`.
- **Провал на забытых таймерах** — уборка идёт в `afterAll`, поэтому провал достаётся файлу, а не
  тесту, а колбэк, запланированный после уборки предыдущего файла, записывается на следующий: счёт
  может уронить файл, который ничего не планировал. Кто на самом деле запланировал каждый, называет
  `timers`, так что включается одной строкой, когда сюита их уже прочитала: `onStrayTimers: 'throw'`
  или `onStrayTimers: ({ timers }) => expect(timers).toEqual([])`.
- **`enableAngularDiagnostics()`** — живёт в `vitest-auto-spy/angular/diagnostics` и требует сначала окружения
  TestBed. Вызывайте его в том же setup-файле как Angular-половину строгости: на Angular-потребителе
  в 1759 файлов он нашёл настоящие дефекты в 25 файлах и 324 тестах и не стоил ничего измеримого
  (12,5 с против 13,4 с на полный прогон).

## Отчёты о неверной настройке, которые падают в месте вызова {#misconfiguration-reports-that-fail-at-the-call}

```ts
setupAutoSpy({ misconfiguration: 'throw' });
```

Библиотека сообщает о неверном использовании собственного API — опечатке в `onlyMethodsToSpyOn`,
`gettersToSpyOn` / `settersToSpyOn` с именем метода, ключе `returns`, на который не отвечает ни один
спай (включая `then` и `constructor` у `createAutoMock`), `injectSpy`, получившем настоящий экземпляр,
записи в `jasmine.DEFAULT_TIMEOUT_INTERVAL`, устаревшем `providedMethodNames`. По умолчанию это
`console.warn`, и несколько из них печатаются один раз, а дальше глушатся — что под `isolate: false`
значило: строку показывал тот файл, который добрался первым. `'throw'` падает в месте вызова, при
каждом повторе, со стеком на той строке, где написана конфигурация.

Оценка общая для процесса — ядро, `/angular` и `/setup` это разные бандлы, поэтому она живёт на
`globalThis` — и снимается после файла, который её поставил.

Печатная оценка тоже поменялась: предупреждение `injectSpy` «got a real …»
теперь глушится для токена **в пределах файла спеки**, а не воркера, так что файл, который его
покажет, больше не зависит от порядка прогона.

## Два буфера, которые вычерпывает уборка {#the-two-buffers-teardown-drains}

Две из проверок выше держат найденное в буфере, пока его оттуда не заберут, и оба буфера
неограниченные.

- **Захваты реджектов.** `trackStrayRejections()` добавляет по записи на каждый проглоченный реджект, и
  каждая запись держит саму причину — `Error`, его стек, а через него и всю цепочку асинхронных
  замыканий, которая его породила.
- **Журнал свойств.** `mockReadonlyProp` и его родня дописывают в
  `globalThis.__vitestAutoSpyPatchedProps__` по записи на патч, каждая из которых держит
  пропатченный объект и перезаписанный дескриптор.

На поддерживаемом пути не растёт ни один: `afterEach` из `setupAutoSpy()` вычерпывает оба после каждого
теста — `flushStrayRejections()` отдаёт массив и оставляет его пустым, `restoreMockedProps()` опустошает
журнал прежде, чем что-то возвращать на место. Копится на всю жизнь воркера только самодельная
половина: `trackStrayRejections()`, который читают только через `countStrayRejections()`, или патчи,
которые читают только через `countMockedProps()`. **Счётчик ничего не опустошает.** Читайте через flush
или отдайте уборку `setupAutoSpy()`, а счётчики используйте ради того ассерта, для которого они и есть.

Поштучная отмена патча тоже ничего не опустошает: она помечает свою запись, а не вырезает её, — иначе
спек, который ставит заглушки в цикле, стал бы квадратичным. Журнал остаётся прежней длины, но на тех
объектах уже ничего не лежит: его длина — это не число живых патчей, и поэтому `countMockedProps()` и
отчёт о патчах, удержанных из прошлого spec-файла, одинаково пропускают помеченные записи.

## Под `test.concurrent` {#under-test-concurrent}

Откаты держатся; атрибуция находки — нет.

Всё, что этот вызов возвращает на место, привязано к тесту, которому принадлежит, и сеть из
раздела 1 теперь помнит, выполнялся ли общий `afterEach`, **на каждый тест**, а не на каждый файл, —
с ключом по объекту задачи, который раннер выдаёт каждому хуку; единственный флаг остался только как
фолбэк для раннера, который задачу не выдаёт. Параллельный сосед больше не может очистить флаг,
который собиралась прочесть сеть первого теста, — раньше из-за этого сеть либо срабатывала для
теста, чей teardown выполнился, либо молчала для того, чей не выполнился.

Обещать нельзя лишь то, какому тесту принадлежит находка. Снимок документа один на весь документ, по
построению. Окно консоли и счётчик unconfigured-read открываются и оцениваются на каждый тест, но
живут в одном слоте — вывод в консоль приходит через глобальный `console`, а не через задачу, так
что привязать его не к чему. Когда в полёте два теста, находка может быть приписана другому или
стёрта до того, как её увидят. Первый concurrent-тест воркера получает ровно одно предупреждение,
которое так и говорит:

```text
[vitest-auto-spy] "CartComponent > loads" runs as test.concurrent, and setupAutoSpy()'s per-test guards judge one test at a time: a console, document or unconfigured-read finding can land on the other test in flight, or be cleared before it is seen.
Run this file's tests sequentially, or give the files that keep test.concurrent a setup with strayConsole: 'off', documentPollution: 'off' and unconfiguredReads: 'off'. Said once per worker.
```

Раз на воркер, не раз на тест. Запускайте файлы, которым нужен гард, последовательно или оставьте
`test.concurrent` файлам, чей setup передаёт `strayConsole: 'off'`, `documentPollution: 'off'` и
`unconfiguredReads: 'off'`.

## Как переставлять заглушку на каждый тест {#reinstalling-a-stub-for-every-test}

```ts
import { installPerTest } from 'vitest-auto-spy/setup';

const observers = installPerTest(() => stubIntersectionObserver({ autoEmit: true }));

it('loads the section once it scrolls into view', () => {
  fixture.detectChanges();

  expect(observers().last.targets).toEqual([host]);
});
```

Любая заглушка, которую ставит эта библиотека, снимается `restoreMockedProps()` после каждого теста —
именно это не пускает её в следующий файл. Следствие легко упустить: заглушка, поставленная один раз на
уровне `describe` или в `beforeAll`, со второго теста исчезает, а падает ассерт про компонент
(«ожидалось 2 вызова, получено 0») — при том что заглушка стоит десятью строками выше и на вид в силе.

Тот же порядок кусает и с другой стороны. Общепроектный setup-файл ставит наблюдателей по умолчанию в
корневом `beforeEach`, а корневые хуки идут **раньше** собственных хуков файла — так что `beforeAll` в
спеке молча им проигрывает, а `beforeEach` в той же спеке выигрывает.

`installPerTest` возвращает **читатель**, а не ручку, потому что ручка в каждом тесте — другой объект:
заглушка, поставленная для предыдущего теста, — это ровно то, до чего не должно быть доступа.
Прочитанный, когда ничего не поставлено, он бросает `installPerTest: nothing is installed yet` и
называет момент чтения — до первого теста (тело `describe`), после того как закончился
`"suite > test"`, или во время `"suite > test"` из хука, зарегистрированного раньше
`installPerTest()`, — с починкой для этого момента.

## Опции {#options}

| Опция                 | По умолчанию | Примечания                                                                                                |
| --------------------- | ------------ | --------------------------------------------------------------------------------------------------------- |
| `duplicateCopies`     | `'throw'`    | `'warn'` — сообщать без падения, `'off'` — пропустить проверку                                            |
| `restoreProps`        | `true`       | `restoreMockedProps()` в глобальном `afterEach`                                                           |
| `propsOutsideHooks`   | `'warn'`     | Сообщать о патче `mock*Prop`, поставленном вне пер-тестового хука, — см. ниже                             |
| `restoreMocks`        | `false`      | `vi.restoreAllMocks()` в глобальном `afterEach` — включать при `isolate: false`                           |
| `strayTimers`         | `false`      | Отслеживать и отменять таймауты, интервалы и кадры, пережившие свой файл                                  |
| `onStrayTimers`       | —            | Принимает число и происхождение отменённого за файл вместо предупреждения в stderr; `'throw'` роняет файл |
| `strayRejections`     | `false`      | Ронять тест, в котором всплыл проглоченный zone.js реджект, — нужен zone.js                               |
| `blockNetwork`        | `false`      | Закрыть все сетевые каналы, какие есть у среды, — `true` или сужающий объект                              |
| `guardGlobals`        | `'off'`      | Сообщать о тесте, переопределившем глобальное свойство как non-configurable                               |
| `prototypePollution`  | `'throw'`    | Снимать и называть enumerable-ключ, оставленный тестом на встроенном прототипе                            |
| `documentPollution`   | `'off'`      | Возвращать и называть атрибут, оставленный тестом на `<html>` / `<head>` / `<body>`, — раздел 17          |
| `strayConsole`        | `'off'`      | Ронять тест (или файл), который писал в консоль и ничем это не поглотил — раздел 16                       |
| `misconfiguration`    | `'warn'`     | `'throw'` роняет собственные отчёты библиотеки о неверной настройке в месте вызова                        |
| `preset`              | —            | `'strict'` запускает каждую охрану на самой строгой оценке — см. выше                                     |
| `globalFakeTimers`    | `false`      | Фейковые таймеры на каждый тест **и между тестами** — см. ниже                                            |
| `restoreTimerGlobals` | `true`       | Возвращать глобалы таймеров, удалённые деинсталляцией фейков                                              |
| `restoreWebStorage`   | `true`       | Дать прогону работающие `localStorage` / `sessionStorage` — см. раздел 14                                 |
| `restoreStorageSpies` | `true`       | Снимать спаи с методов Web Storage, которые не может `mockRestore()`, — прокси happy-dom, раздел 18       |
| `strayListeners`      | `false`      | Снимать слушатели `window` / `document`, оставленные файлом, — раздел 19                                  |
| `onStrayListeners`    | —            | Принимает число за файл и происхождение каждого вместо тихого подметания; `'throw'` роняет файл           |
| `restoreGlobals`      | `false`      | Возвращать каждый изменённый глобал `globalThis` на границе файла — раздел 20                             |
| `pruneMockRegistry`   | `false`      | Держать вечно растущий реестр моков @vitest/spy в рамках моков, переживающих файл                         |
| `hookTimeoutHint`     | `true`       | Объяснять хук, исчерпавший `hookTimeout`, когда `testTimeout` больше                                      |
| `frozenClockHint`     | `true`       | Объяснять таймаут, случившийся потому, что фейковые часы никто не прокрутил                               |
| `angularBuildHint`    | `true`       | Сказать раз на воркер, что `@angular/build` собирает тестовый бандл без сплита                            |
| `strict`              | `false`      | Каждый построенный после этого дубль бросает на ненастроенном методе                                      |
| `onUnstubbedCall`     | —            | Общая форма `strict` — её возвращаемое значение становится результатом вызова                             |
| `unconfiguredReads`   | `'off'`      | Сообщать о геттере строгого дубля, прочитанном, или потоке, на который подписались, без настройки         |
| `onUnstubbedRead`     | —            | Забирает эти находки вместо отчёта, со всех дублей — для обследования                                     |

`restoreMocks` выключен по умолчанию, потому что он заодно сносит заглушки `vi.spyOn`, поставленные
сюитой в `beforeAll`; это ручка, за которую берутся, когда прогон делит одну среду между файлами.

```ts
setupAutoSpy({ restoreMocks: true, duplicateCopies: 'warn' });
```

## Патч, поставленный не в тот хук, перестаёт действовать {#a-patch-put-in-the-wrong-hook-stops-applying}

```ts
describe('modal', () => {
  const modal = new Modal();

  mockValueProp(modal, 'onClose', () => 'patched'); // ← отрабатывает один раз, на сборе

  it('one', () => expect(modal.onClose()).toBe('patched')); // ✅
  it('two', () => expect(modal.onClose()).toBe('patched')); // ❌ 'real'
});
```

`restoreProps` снимает патч `mock*Prop` после теста, **во время которого патч оказался применён**, —
независимо от того, когда его создали. Патч, написанный в теле `describe` или в `beforeAll`, поэтому
переживает ровно один тест, и обратно его никто не ставит. Первый тест проходит, все следующие читают
настоящий член, а падение прилетает как `… is not a function` далеко от строки, которая всё это
устроила. Найдено сразу в шести файлах одной сюиты при массовом переводе на `mockValueProp`.

Починка в одну строку: перенести вызов в `beforeEach`, где патчу, нужному каждому тесту, и место.

`setupAutoSpy` теперь говорит это вслух, а не оставляет узнавать отладкой. `propsOutsideHooks`
задаёт резкость: `'warn'` по умолчанию, `'throw'` для сюиты, которая предпочтёт упасть на первом
тесте, `'off'` — для той, что считает свои патчи в `beforeAll` своим делом:

```ts
setupAutoSpy({ propsOutsideHooks: 'throw' });
```

```text
[vitest-auto-spy] mockValueProp(…, 'onClose') on an object in src/app/modal.spec.ts ran outside a per-test hook, so the sweep after the first test took it off for good and every later test reads the real member.
Move the call into beforeEach, so it is applied again for each test.
Docs: https://asdalexey.github.io/vitest-auto-spy/utilities/setup#a-patch-put-in-the-wrong-hook-stops-applying
```

Вызов пишется с первым аргументом, когда у того есть имя, — `globalThis`, `document`, функция или
прототип, — а иначе описывается после вызова (`on an object`, `on a Modal`).

Сюите, которая заводит хуки сама, а не через `setupAutoSpy`, тот же переключатель ставит
`reportPropsOutsideHooks(reaction)`; тип реакции экспортируется как `OutsideHookReaction`.

Сообщение выходит один раз на объект и свойство в каждом spec-файле, поэтому патч из тела `describe`
называется однажды, а не на каждом тесте. Именно на файл, а не на воркер: под `isolate: false` учёт на
весь воркер называл патч общего объекта только в том файле, который случайно шёл первым. Ключом служит
объект, а не имя, потому что два файла одного воркера сплошь и рядом патчат член с одинаковым именем на
разных объектах, и отчёт по имени назвал бы первый и замолчал бы про второй.

**Почему патч не переустанавливается сам**, хотя починка выглядит именно так.
`restoreMockedProps()` существует затем, чтобы патч не пережил свой файл; патч, возвращающий себя на
каждом тесте, ровно это и сломал бы под `isolate: false`, где «файл, которому патч принадлежит» —
не то, что журнал способен увидеть. Здесь убирают молчание, а не правило.

Это второй хелпер пары — первым был `restoreMocks: true`, снимавший спаи на аксессоры с дубля,
построенного где угодно, кроме `beforeEach`. Тот случай починили молча, потому что законного смысла у
него не было вовсе; у этого смысл есть и починка в одну строку — поэтому о нём сообщают.

## Фейковые таймеры на весь прогон {#fake-timers-for-the-whole-run}

```ts
setupAutoSpy({ globalFakeTimers: true }); // или объект конфигурации `vi.useFakeTimers()`
```

У Jest было `fakeTimers: { enableGlobally: true }`; у Vitest такой настройки нет. Сюита, приехавшая из
проекта, который ею пользовался, писалась против замороженных часов везде, а включать это обратно
файл за файлом — тысяча правок.

Оба конца защищены, и как раз эту половину пара самописных хуков делает неправильно. Спека, которая
сама крутит часы, иначе доехала бы до второго `vi.useRealTimers()`, а под happy-dom он оставляет среду
без `clearInterval` — что взрывается во время уборки того файла, который пойдёт следующим, и обвиняет
его.

Он также держит часы фейковыми **между** тестами — та половина `enableGlobally`, которую пара только
из `beforeEach` упускает: `beforeAll` внутри вложенного `describe` выполняется _после_ `afterEach`
предыдущего теста, так что блок, готовящий там свои образцы, иначе встретил бы настоящие таймеры и упал
бы с `the timers APIs are not mocked` — в наборе, чьи собственные тесты таймеров вообще не трогают.
Фейки снимаются насовсем в `afterAll`, так что они никогда не переживают файл. Для одного `describe`, а
не для всего прогона, это [`setupFakeTimers(config, { betweenTests: true })`](./fake-timers).

Передавайте объект конфигурации, а не `true`, когда прогон гоняет настоящий HTTP-обработчик: `toFake`
по умолчанию включает `setImmediate`, а это и есть способ, которым `404` в Express превращается в
30-секундный таймаут, не называющий ничего, — см.
[как убрать `setImmediate` из `toFake`](./fake-timers#taking-setimmediate-out-of-tofake).

## Общие фикстуры — это функции, а не константы {#shared-fixtures-are-functions-not-constants}

При `isolate: false` модуль вычисляется **один раз на воркер**. Экспортированный объект, держащий
`vi.fn()`-ы, — это, стало быть, один набор спаев, общий для всех импортирующих файлов, зарегистрированный
на тот файл, который добрался первым, и `clearMocks` остальных файлов до них не достаёт. Симптом —
30-секундный таймаут на каждом прогоне в разных файлах.

```ts
// ❌ константа: один набор спаев на весь воркер
export const mockActionContext = { actions: { navigateToSection: vi.fn() } };

// ✅ фабрика: по набору на каждого вызывающего
export const createActionContext = () => ({ actions: { navigateToSection: vi.fn() } });
```

То же касается общей фикстуры провайдера — `{ provide: X, useValue: { load: vi.fn() } }` остаётся
константой, пока его не возвращают из функции. А файл спеки не должен **экспортировать ничего**: при
`isolate: false` экспортирующий файл спеки импортируется соседями и теряет собственную сюиту. Общие
дубли кладите в `*.mock.ts` рядом с ними.

Правило [`no-shared-module-level-mock`](/ru/utilities/eslint-plugin) находит такое механически, а это
быстрее, чем таймаут, которым они находятся иначе.

## Порядок хуков отличается от Jest {#hook-order-differs-from-jest}

Vitest выполняет хуки `afterEach` как стек — самый внутренний и зарегистрированный последним идёт
первым, — тогда как Jest выполнял их в порядке объявления. В переехавшей сюите, где `afterEach` спеки
зависит от того, что поставил setup-файл, уборка setup-файла теперь идёт первой, и хук спеки работает
над уже восстановленной средой. `sequence: { hooks: 'list' }` в конфиге Vitest возвращает порядок Jest.

## Хуки принадлежат тому файлу, в котором выполнился этот вызов {#the-hooks-belong-to-the-file-this-call-ran-in}

Всё, что ставит `setupAutoSpy()`, — это `beforeEach` / `afterEach` / `afterAll`, а хук,
зарегистрированный во время импорта setup-файла, принадлежит тому файлу спеки, при сборе которого этот
импорт случился. Vitest переимпортирует setup-файлы для каждого файла спеки, так что обычно ничего из
этого не видно.

Видно становится, когда что-то держит setup-модуль в кэше модулей между файлами: вызов происходит один
раз, и у каждого файла после первого в этом воркере нет ни одного из хуков — ни восстановления свойств,
ни `blockNetwork`, ни отмены забытых таймеров, ни `restoreTimerGlobals`, ни глобальных фейковых
таймеров. Никто об этом не сообщает, а симптом всплывает совсем в другом месте — утёкший глобал или
`A function to advance timers was called but the timers APIs are not mocked` в спеке, которая зелёная,
когда её запускают отдельно.

Случай, встреченный в дикой природе, — `@angular/build:unit-test` **до 22.2.0, с покрытием**. Билдер
тогда отдаёт каждый тестовый файл — setup-файлы тоже — как обёртку, импортирующую собранный бандл,
setup-модуль остаётся разрешённым в общей среде, и его верхний уровень больше не выполняется; без
покрытия тот же прогон в порядке, отчего это и читается как «покрытие сломало тесты».
**`@angular/build` 22.2.0 это исправляет** (angular-cli PR #34143): setup-файлы больше не
заворачиваются в обёртку, и их хуки регистрируются для каждого файла спек и под `--coverage`.

На более старом билдере два выхода: гонять покрытие с пофайловой изоляцией
(`ng test <project> --coverage --isolate` или `isolate: true` в конфиге только для этого случая —
билдер сохраняет `test.isolate` из конфига раннера, если у таргета не задана собственная опция
`isolate`) либо
вызывать `setupAutoSpy()` из чего-то, что вычисляется на каждый файл, а не из модуля, который раннер
может закэшировать. На любой версии держите вызов на верхнем уровне самого setup-файла: модуль,
который setup-файл импортирует ради побочных эффектов, всё ещё может оказаться в общем чанке и
выполниться один раз на воркер.

## Setup-файл, которому досталась собственная копия Angular {#the-setup-file-that-gets-its-own-copy-of-angular}

Setup-файл, вызывающий `initTestEnvironment` — напрямую или через тот пресет, который достался
воркспейсу по наследству, — выполняется раньше любой спеки, а значит и раньше, чем импортируется
библиотека. Там, где две половины прогона разрешают `@angular/core` по-разному, одного этого порядка
достаточно, чтобы в процессе оказались две копии Angular: платформа, окружение и `TestBed`, собранные
setup-файлом, принадлежат одной из них, а каждая спека строится на другой.

То, что всплывает, о копиях не говорит ничего. Одна половина — вот это:

```text
[vitest-auto-spy] No mock adapter registered: a spy was built before 'vitest-auto-spy' was imported. This is Vitest — import the factories from 'vitest-auto-spy', in the spec or once in the `setupFiles` entry.
Docs: https://asdalexey.github.io/vitest-auto-spy/core/installation#vitest
```

Реестр — это состояние модуля `vitest-auto-spy`, а пишет в него именно импорт точки входа, так что
прогон, держащий две копии библиотеки, может выдать спеке ту копию, которую никто не импортировал.

Вторая половина — `TestBed`, который ведёт себя как чужой: `configureTestingModule` принят, а
компонент всё равно получает настоящий сервис; `overrideProvider`, который так и не применяется;
`NG0203` из контекста инъекции, выглядящего совершенно обычным. Второй `TestBed` с собственным
инжектором объясняет все три симптома, и ни один из них об этом не говорит.

Чинится это в конфиге раннера, а не в спеке:

```ts
// vitest.config.ts
export default defineConfig({
  resolve: {
    dedupe: ['@angular/core', '@angular/common', '@angular/platform-browser', '@angular/compiler', 'rxjs'],
  },
  test: {
    server: { deps: { inline: ['vitest-auto-spy'] } },
  },
});
```

`dedupe` заставляет всех импортирующих разрешать эти пакеты в один файл, в каком бы вложенном
`node_modules` он ни лежал. Инлайн библиотеки отправляет её через тот же конвейер трансформации,
которым идут спеки, вместо того чтобы оставить её вынесенной в Node, — и копия, которую грузит
setup-файл, и копия, которую грузит спека, становятся одним экземпляром модуля: один реестр, один
набор спаев. `rxjs` попадает в этот список по той же причине, что и пакеты Angular: `Observable` из
одной копии не проходит `instanceof` другой, а это уже другая поломка с той же причиной.

`test.server.deps.inline` — место этого ключа начиная с Vitest 1; проверено здесь по типам 5.0.
Верхнеуровневый `test.deps.inline`, который всё ещё показывают старые ответы, переехал туда и больше
не существует.

Сначала загляните в отчёт о дубликатах из раздела 2. Вторая установка или одна установка, загруженная
и в ESM-, и в CommonJS-форме, — причины более частые, и `dedupe` не нужен ни одной из них: отчёт
называет обе копии, а этот раздел — про то, что делать, когда напечатанные им пути ведут к одному и
тому же пакету, разрешённому дважды.
