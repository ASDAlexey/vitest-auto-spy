{
  "name": "vitest-auto-spy-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Documentation site for vitest-auto-spy (VitePress).",
  "type": "module",
  "scripts": {
    "predev": "node ../scripts/sync-ru-pages.mjs",
    "dev": "vitepress dev",
    "prebuild": "node ../scripts/sync-ru-pages.mjs && node ../scripts/generate-llms-txt.mjs",
    "build": "vitepress build",
    "postbuild": "node ../scripts/check-docs-links.mjs",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
